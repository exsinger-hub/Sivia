Create an original scientific overview titled “DC²: inspect locally, reason globally”. Explain a high-resolution image-understanding method through recursive image division, local description, cross-level object filtering, coordinate memory and query-guided retrieval. The source is the AAAI 2025 Divide, Conquer and Combine framework. The new scene is an illustrated botanical courtyard with a blue umbrella. It is an explanatory example, not an HR-Bench image, a measured model answer or a benchmark result.

COMPOSITION AND MEANINGFUL DENSITY

Use a landscape 1.7:1 canvas with narrow two-percent outer margins. Create three unequal vertical zones: Divide 29%, Conquer 39%, Combine and answer 28%, with only small routing gutters. A shallow title spans the top. Each zone should be filled by a hierarchy of image windows and short technical carriers. Keep the regions tall enough that the final objects align near the same baseline. The smallest labels must remain legible; no bullet paragraphs inside the figure.

The left zone is dominated by an original large courtyard image and its crop tree. The middle zone contains two vertically aligned description scenes for a parent and selected child views, plus a compact object-set intersection directly below them. The right zone contains a substantial coordinate-memory board and a query-to-retrieved-patch-to-answer path. The figure should read left to right at first glance, and then vertically within each stage. Do not add a second full pipeline row at the bottom. Use that area for large useful memory and crop representations.

SOURCE-GROUNDED METHOD CONTRACT

Divide recursively partitions an image into four quadrants and merges similar patches to reduce redundant processing. Conquer produces local descriptions and object sets; non-leaf descriptions can use child descriptions. Combine retains objects found at adjacent levels, stores patch coordinates and retrieves relevant image patches when a query asks about them. Original pre-merge coordinates remain associated with merged content, and overlapping object patches can be reduced with NMS. These operations must not be replaced by a single opaque “LLM” box.

The figure shows one expanded recursive branch and symbolic omitted branches, not an exact full execution trace. The hierarchy is conceptual. Do not invent a crop-size threshold, clustering threshold, model variant, latency, accuracy, number of actual requests or measured confidence. The example answer is specified by the artist's blue umbrella and must be visually marked as an illustrative answer. No learned heatmap or empirical plot appears.

THE COURTYARD IMAGE

Draw a close, richly detailed but uncluttered view of a botanical courtyard. A conservatory building with pale stone walls occupies the upper-left region. A glazed roof and a few potted plants occupy the upper-right. A clearly blue open umbrella stands over a small round table near the lower-right center. A low stone fountain and a pathway occupy the lower-left. Keep the umbrella well inside the frame, with a visible pole and a consistent blue canopy. It is the query target. Fill the image with these meaningful spatial objects, without a large sky band or an empty paved foreground.

The image has a thin neutral border and the label “High-resolution image”. Overlay one thin white or coral vertical divider and one horizontal divider to show four equal quadrants. The quadrant boundaries are geometric crop boundaries, not segmentation outlines. Put small corner labels q1, q2, q3 and q4 outside the actual scene so they do not become text painted on the building. The same courtyard, table, umbrella, fountain and plant arrangement must remain recognizable in every crop. Do not replace the scene with a different location in the lower panels.

DIVISION AND SIMILARITY MERGING

Immediately below the large image, place four equal thumbnails showing exactly its four quadrants. Use the same q1–q4 order, left to right, with thin source-to-crop association lines from the parent frame's lower edge. These lines do not imply that crops are produced sequentially. The four thumbnails are parallel children of the same parent. The top-right and bottom-left crops may contain similar pale paving-and-plant textures, but their original coordinate identities must remain distinct.

Below the four thumbnails, show “Merge similar patches” with a compact grouping bracket joining two similar child thumbnails into a single grouped carrier. Preserve both small coordinate tags under that carrier. This is similarity-based merging, not image stitching into a new physically continuous photograph. Depict the merged carrier as two offset thumbnail faces with a small “group” label or an average-feature slab, not as a fabricated panorama. A short operator label “Similarity clustering” sits beside the grouped thumbnails, without a numeric threshold. Label the displayed grouping Illustrative merge: q2 + q3; no bracket should accidentally include q1.

Use the remaining lower-left space to expand the umbrella-containing child into four smaller crop tiles. Connect that chosen child's bottom port to a visible branch dot and four short outgoing crop lines. The other first-level children end in small ellipsis markers labeled “Other branches”. Make the expanded crop tree occupy the region: do not draw a tiny tree above a large empty box. The child tile containing the blue canopy is outlined in blue and marked “Target patch”. It will remain the same patch in the memory and retrieval scenes.

Add a narrow vertical “Original coordinates” strip along the left side of the crop tree. A small global-image outline with a highlighted rectangle conveys that the saved bounding box belongs to the uncropped global coordinate system. Use only symbolic coordinates “(x, y, w, h)”. Draw x/y arrows from the global image's top-left origin to the patch corner and short w/h brackets around the highlighted rectangle. Do not insert fabricated pixel values or accidentally use an xyxy notation under an xywh label.

LOCAL DESCRIPTION SCENES

At the top of Conquer, place a wide parent-image thumbnail showing the whole courtyard in a compressed but readable view. It enters a compact operation “Describe parent”. On the operation's right, show a short description card: “Courtyard with a building, fountain and blue umbrella.” This sentence is a newly authored illustrative description. Under it place an object-chip row with “building”, “fountain”, “umbrella”. Label the card “Parent description” and the chips “Parent objects”. The model operation and its outputs should be visible, not hidden in a document icon.

Below this parent scene, place two substantial child-image thumbnails: the umbrella patch and a building/fountain patch. Each retains its actual crop of the courtyard. They enter “Describe children” through separate input ports. To the right, show two short cards, “Blue umbrella over a table.” and “Stone building beside a fountain.” Under these cards place a compact combined object-chip row “umbrella”, “table”, “building”, “fountain”. Label it “Child objects”. Do not produce a dense paragraph of invented model narration.

Label the parent operation Describe(parent image, child text). This explicit input signature shows that the non-leaf description uses both its image and child descriptions. Do not draw a vertical return arrow between the two scenes. The child operation is Describe(child images), with its two adjacent input thumbnails clearly grouped and labelled Child images. Parent and child scenes use neutral model blocks and coral/blue object identity accents.

For a leaf patch, the description uses the patch image alone. A small short label “Leaf: image only” sits beside the umbrella crop. For a non-leaf node, child text supplements the image; a label “Parent: image + child text” sits beside Describe parent. These two labels make the scope explicit without a second full hierarchy diagram. No teacher, training loss, gradient arrow or fine-tuning stage belongs in this training-free inference illustration.

CROSS-LEVEL OBJECT FILTER

Label the Parent objects row O_l and the Child objects row O_l+1. Keep them distinct. A compact operator Intersection(O_l, O_l+1) sits to their right; its signature names the two operands. Do not draw incoming arrows from the rows into the operator. Show the three common object chips “building”, “fountain”, “umbrella” continuing to a retained row. The child-only “table” chip sits to the side in a faint neutral outline. It is excluded by this example's adjacent-level agreement rule; do not suggest that tables do not physically exist in the scene or that the method proves semantic truth. Use the concise label “Cross-level agreement”.

Print the symbolic relation “Ōₗ = Oₗ ∩ Oₗ₊₁” immediately beneath the two rows. Keep the hat, subscripts and intersection symbol legible. This is a set intersection, not averaging, a union, a learned gate or an attention matrix. The Intersection operation has one short output arrow to the retained object chips. Name the retained set O-hat_l. The memory construction is labelled Store(O-hat_l, pre-merge coordinates), making its two inputs explicit without any long connecting line across panels. Do not draw bottom-edge coordinate or object buses. Object names and coordinates remain distinct fields in each memory row.

Avoid turning this scene into a Venn diagram with large nearly empty circles. Use compact aligned chips and a central set-operation glyph. The graphic should occupy most of the local width through actual operands and correspondence. The background remains white or a very light tightly fitted grouping tint; it does not count as scientific information.

COORDINATE MEMORY

At the top of the right zone, draw a substantial memory board titled “Visual memory M”. It contains three horizontal rows, one per retained object. Each row has an object chip, a small registered image crop and the symbolic coordinate field “(x, y, w, h)”. Use building, fountain and umbrella rows, with the umbrella row highlighted in blue. The tiny crop is a real part of the conceptual courtyard image, not a generic icon. The coordinate text denotes patch coordinates in the global image, not a segmentation boundary or camera pose.

Beside the memory rows, show a compact pair of overlapping patch rectangles in the global courtyard outline. A small operation “NMS for overlap” leads to one retained rectangle. This is a supporting storage cue, not a third candidate-selection algorithm for answers. Preserve the phrase “Pre-merge coordinates” beside the coordinate-entry port. Multiple original coordinates can stay associated with merged content; a merged patch cannot be assigned a newly invented bounding box covering disconnected image pieces as though they were one contiguous crop.

Keep the memory board relatively tall and filled with the three rows. Avoid drawing a large empty database cylinder with only the letter M. The rows make the memory's contents clear at reading distance. The board's output is a query-selected patch, not a direct final answer. Draw a separate query port on its lower edge, with enough clearance that the query arrow does not look like a stored object name.

QUERY AND ANSWER

Directly below memory, place a short user-question card Q reading What color is the umbrella? The word umbrella uses the blue identity accent. Beside it put the operation Retrieve(M, Q). Its explicit input signature means that the memory and question are consumed together. Draw one short arrow from Q to Retrieve(M, Q), then one arrow from Retrieve(M, Q) to a large image frame R labelled Retrieved patch. Do not draw a direct Q-to-image bypass or long data lines from memory rows. The frame R contains the identical blue umbrella crop seen in the tree and in the umbrella memory row.

R enters a compact operation Describe(R), producing a short text card T: The umbrella is blue. Below this, an operation Answer(Q, T) has one short output arrow to Blue. The signature explicitly names question and retrieved text as its two inputs. Print Illustrative answer beside Blue. Do not draw a long extra Q route around the panel. The linear retrieval-description-answer sequence and its named operands fully state the dependencies without crossing lines.

Keep the output sequence tight enough to fill the lower-right zone. The enlarged umbrella crop should be the visual focus of this local scene, with operation labels arranged beside it rather than consuming all the area. Do not add confidence bars, green pass badges or numerical answer quality. The answer is visually grounded in a deliberately blue conceptual object and is not an executed evaluation.

STYLE AND CORRESPONDENCE

Use coral for division/crop boundaries, blue for local perception and target-object identity, and green for memory/combination. Use a consistent gray-blue technical text palette. The umbrella remains visibly blue in every frame; source-to-crop correspondences remain geometrically correct. Crop tree edges are thin solid lines with clear branch dots, model data edges are slightly heavier arrows, and source association guides are fine dotted lines. Do not draw arrows through the photographic-style image fields.

Print the title, stage headings, short operation labels and exact example text specified above. Print no full source-paper title, reference list, numeric metric or model self-evaluation. Add the small footer “Conceptual illustration”. The new image should be rich because the same concrete scene is tracked through crops, descriptions, sets and retrieval, with low unused canvas area and a fully recoverable scientific story.


RENDERING CONTRACT FOR THE SPECIFIED SCIENTIFIC SCENE

Render one complete image from the fully instantiated scientific specification above. The specification is a production document: its paragraphs explain how to place and connect visible objects. Print only the short labels, mathematical expressions and example text explicitly identified as visible. The final artwork is an explanatory method illustration with a small readable footer saying “Conceptual illustration”. This footer should occupy a thin baseline immediately below the last content row. It must not require a separate panel or leave a large empty strip. Do not print the name of this rendering contract or any authoring directions.

DENSITY AND USEFUL CONTENT

The composition must have low uninformative empty area. Use narrow exterior margins of approximately two percent of the canvas width and height, and compact internal gutters. A gutter is a short gap separating two objects or carrying a connection; it is not an empty column between independent panels. Let the scientific scenes reach close to the four sides of their allotted regions. Where a region contains fewer objects, enlarge the actual representation and give its internal structure room to be understood. Resize an entire meaningful scene coherently, including its labels and ports, instead of enlarging a heading or adding a blank tinted container.

Maintain continuous scientific content across the body of the composition at both coarse and fine viewing scales. No large region should consist only of a flat background, a border, one arrow, or a single oversized word. Meaningful visual mass comes from the source-defined inputs, recognizable scene elements, spatial correspondences, ordered representations, operator operands and genuine outputs. Color alone does not create information. A pale colored area between disconnected boxes is still empty. If the proposed arrangement leaves such an area, pull the connected objects together and use the recovered room to enlarge an existing input field or mechanism inset within its scientific scope.

Design the body so that every major horizontal and vertical band intersects useful representations. Offset adjacent scenes only when doing so creates an intelligible hierarchy or permits a short route. Avoid a row of small boxes aligned along the top of a large empty card. Avoid a tall central feature stack surrounded by white triangles. Use the exact proportions given in the case specification, then fit local geometry tightly within them. A pair of corresponding panels should have aligned view windows; a panel with a different function may have a different width. Equal panel sizes are not a default.

Retain functional breathing room at arrowheads, formula baselines and sample boundaries. Dense should mean that the reader sees more scientific relationships, not that objects touch arbitrarily. Leave at least one small text-height of clearance around a central merge symbol and enough gap for an arrowhead to be seen as an endpoint. Do not reduce important labels to hairline text to squeeze in prose. If there is a fit conflict, shorten a permitted local label through line breaks or reduce the space between its graphic operands. Do not silently omit a required input or collapse distinct operations.

SCIENTIFIC GLYPHS AND IMAGE FIELDS

Construct each image field as a deliberate rectangular viewport containing the specified scene. Its crop must include the objects needed for the illustrated operation; remove gratuitous sky, blank walls or empty floor that would make the frame seem sparse. A viewport is a scientific carrier, so its area may be filled by scene texture only when that texture belongs to the declared scene. Keep matched fields registered to the same conceptual sample: maintain object count, orientation, silhouette, relative position and color whenever the operation preserves them. A detail crop has a visible association to its source field and cannot become a newly invented sample.

Make symbolic feature fields visibly different from natural image fields. Feature planes may use small colored cells, diagonal hatching or a few grouped marks to suggest channels and positions, but do not present them as computed activation maps. Use clean abstract patterns with no colorbar, decimal values or heatmap performance legend. A token strip has a clear reading order and short identity markers. A spatial grid has a clear two-dimensional arrangement. A graph has nodes and edges with a declared adjacency meaning. These carriers should remain identifiable in grayscale by shape, enclosure and annotation, not just color.

Use only the number of illustrative cells or nodes specified in the scene. When a display samples a larger set, an ellipsis at the end of the local strip indicates omitted repeated items, and the set label remains symbolic. Do not write a precise channel count or number of layers unless the case explicitly provides it as a verified quantity. If a stack represents scales, vary its face size while keeping the same image identity. If it represents channels, hold the face size constant and use a small depth offset. If it represents time, use a sequence of matching panels and indicate the direction once.

Treat masks as meaningful spatial supports. The mask viewport must have the same aspect ratio and object registration as its corresponding image viewport. White and black encode the declared binary convention; do not invert that convention in a neighboring panel. A missing-data mask is not automatically a target segmentation mask. A difference mask represents the specified comparison of supports, not uncertainty, error probability or confidence. Keep masks visually flat and crisp, with no decorative lighting or translucent photographic texture inside their binary support.

LOCAL CONNECTION GEOMETRY

Every scientific edge has one identifiable source and one identifiable destination. Start a main arrow at a representation boundary or an operator output port, and terminate at the input side of the operation that consumes it. Do not terminate a data arrow on a panel title. At a split, one short trunk reaches a visible branch dot, then separate branches depart toward their actual targets. At a merge, separate operands remain visually independent until they reach the explicit merge symbol. Mere proximity or placement inside the same enclosure does not communicate a dependency.

Use short orthogonal routes for long-distance data connections and a gentle bend only where it prevents a crossing. Reserve slim routing lanes along the edge of a region, but keep them adjacent to the content they serve. A route should not travel around the entire figure unless the source-defined feedback truly returns to an early stage. Even a return edge should use one compact outer lane with a clear arrowhead at its consumer. Distinguish a data return from a dotted explanatory association to an expanded inset. The inset association has no arrowhead suggesting that the inset computes an extra downstream result.

If two independent lines must cross, use a small bridge or a clean uninterrupted crossing without a junction dot, and keep the crossing away from labels. Junction dots indicate actual joins or branches only. Avoid running connectors directly beneath text, through image viewports or behind a large group header. A line hidden by an operator box cannot be assumed to connect to it. Give each multiply, sum or concatenate operation a distinct visible glyph or a short operator label. Addition, concatenation, selection and conditioning must not share an unlabeled circular node.

Align parallel branches at their entry and exit ports so the eye can compare their operation order. Do not force a parallel architecture into a single serpentine chain. When two paths share a representation, draw a branch from that representation; when they use independent representations of the same type, draw separate objects. A repeated module name does not mean weight sharing, and a repeated sample icon does not mean a new sample. Use the identities and scope markers provided in the case specification to make these distinctions visible.

TYPOGRAPHY AND HIERARCHY

Use one clear sans-serif family with a slightly heavier weight for panel headings and operation names. Mathematical symbols may use a compatible serif math face, but their baseline and optical weight should sit comfortably with the neighboring technical label. Set the main title at about one and a half times the body-label size, not several times larger. Panel headings are only modestly larger than body labels. Keep the title height shallow so that the body begins near the top edge. The scientific scene is the visual focus.

At the intended full-image display size, the smallest necessary technical labels must remain comfortably readable. Use a limited number of short lines inside a document or description card; do not simulate paragraphs with tiny illegible text. Place a text label immediately next to the object it names. Use a maximum of two lines for ordinary operation names, with line breaks at semantic boundaries. Avoid stretching letter spacing to fill empty width. Important index letters, a prime on a forecast horizon, and the distinction between a feature value and a positional label must be legible.

Place tensor or set notation directly beside the representation. Put units or axis roles beside the axis they describe. A legend should be small, close to the relevant scene and used only when the same visual grammar recurs enough to justify it. Do not repeat a full legend in every panel. Do not give illustrative token shades a numeric scale. A concise note such as “symbolic”, “example”, “training” or “inference” can establish scope where the case requests it, but internal evaluation status and production bookkeeping stay outside the artwork.

COLOR, MATERIAL AND STROKE

Follow the case's role-based palette. Use white as the main canvas and restrained light fills behind tightly fitted groups. Use dark charcoal for body text and main outline work. Give each scientific family one accent and a darker version for its border or port. An input image may naturally contain several colors without redefining the semantic palette of model components. Colors used to track an object through a transformation must stay stable across all views of that object.

Use consistent medium-weight strokes for image frames and operator boundaries, slightly heavier arrows for the main pathway, and thinner dotted strokes for explanatory associations. Avoid thick black outer borders that visually divide the canvas into disconnected boxes. Rounded corners may be subtle for grouping, but flat operator blocks and planar fields should dominate. Small offset planes may create modest depth where they explain a representation stack. Do not add glossy reflections, floating shadows, lens flares, decorative neural-network backgrounds or ornamental circuitry.

For visual depth in scene thumbnails, use clear foreground/background separation and enough local contrast to show the designated object. Keep lighting consistent between matched images. A changed illumination or camera viewpoint should not masquerade as the output of an algorithm that preserves geometry. Where the specification calls for a physical object silhouette, draw that silhouette precisely rather than substituting an unrelated stock icon. Where it calls for an abstract state, do not replace the state with a face, a robot or a generic brain symbol.

COMPLETENESS AND FINAL FIT

Ensure the reader can follow the entire specified input-to-output path without consulting a companion figure. Routine operators may be compact, but the distinctive transformation needs visible operands and a changed representation. Where the case includes a supporting mechanism inset, keep that inset visually subordinate to the main path while making its correspondence unambiguous. The inset should explain an operation already present in the main pathway; it must not introduce a new unverified component just to increase complexity.

Inspect the intended drawing as three nested readings. The thumbnail should show a coherent scientific scene with low unused area. At ordinary reading distance, module roles, branches, scope changes and outputs should be apparent. At closer inspection, the exact object identities, mask correspondence, attention axes or source-defined equations should be recoverable. These readings must agree; an attractive silhouette does not excuse an incorrect arrow or an output that changes object identity.

Finish with a clean edge-to-edge arrangement of the required content. Remove oversized spaces beneath terminal outputs, around tiny formulas and above small panel headings by refitting the real content locally. Preserve the necessary connector clearances while doing so. Keep all important labels inside the canvas, all image fields fully visible and all required arrows unobstructed. Render the complete illustration at high resolution with an opaque white background. The final deliverable is one scientific conceptual figure, not a contact sheet, a presentation slide with bullet paragraphs, or a screenshot of these production instructions.
