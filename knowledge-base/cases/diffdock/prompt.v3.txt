PRECISION EDIT of the provided second DiffDock image. Keep the dense molecular layout and the correct central Current pose→Reverse update→Sampled poses→d chain. Fix three local routing groups only. First, DELETE the long vertical line along the LEFT of sampling s that drops down into the protein-y tab beside inference d; that tab is an independent local condition and has only its short arrow into d. Second, in upper-right score training DELETE the chained y→t glyphs and their wires, and use only the explicit function signature s(x_t,y,t) plus a static note “conditioned on y, t”. No direct x_t→Score matching edge is allowed. Predicted scores and kernel scores are the two loss operands. Third, in lower-right confidence training REPLACE the separate Pose error box, free threshold formula, and bottom-right b square with ONE compact target-computation box beneath the two molecular fields: “Pose error target” over “b = 1[RMSD(x,x*) < ε]”. Both fields feed this box on opposite sides. Its bottom edge sends a short arrow directly into the TOP of Cross-entropy. DELETE the entire long x*→b vertical wire and the obsolete separate b square. Preserve d→Cross-entropy from the left. No other relayout, extra panels, or numerical results. The full self-contained overview specification follows.

Create a complete revised DiffDock METHOD OVERVIEW, preserving the tight white 4:3 composition, molecular visual language, left motion glyphs, vertical ensemble story, bottom ranked poses and right training scope of the supplied first draft. Repair the local scientific routing and ligand-identity issues specified in this full prompt. This is a complete overview specification, not a change-only instruction. Retain the small DiffDock name and Conceptual illustration label. Scientific objects must continue to occupy the canvas densely, without adding banners, unused cards or extra modules.

SCIENTIFIC AUTHORITY AND THE SINGLE VISUAL ARGUMENT

Treat the following method description as authoritative. The source is the ICLR 2023 DiffDock paper, arXiv 2210.01776v2, especially its Figure 1, Sections 4.1–4.5, and the implemented approximate training and inference procedures in Appendix B, Algorithms 3 and 4. The docking input is a seed ligand conformation c and a protein structure y. The protein is the fixed spatial reference. Pose sampling acts on the ligand's translational, rotational, and torsional degrees of freedom, not on arbitrary independent atomic coordinates. The score network takes the current noisy ligand pose x_t, protein structure y, and diffusion time t. It returns translational and rotational vectors and a scalar torsional score for each rotatable bond. Reverse-diffusion updates repeatedly modify the current ligand pose. An independent confidence network receives a completed sampled pose together with the protein structure and ranks candidate poses. Confidence refers to pose accuracy within a structural error tolerance; it is not binding affinity, chemical potency, energy, therapeutic efficacy, or an experimentally verified result.

The image is an overview of one method with a local explanatory expansion, not a collection of unrelated molecule graphics. The motion glyphs are explanations of three concurrently available degrees of freedom inside each reverse step. They are not three algorithm stages that must be executed in the order translation, rotation, torsion. A small recurrence label can indicate repeated updates; do not introduce an unverified number of diffusion steps. A few candidate identities A, B, and C are illustrative identities used to follow the same candidates across drawings. Their count is not a prescribed sample budget. A, B, and C have the same molecular connectivity and atom inventory but different poses. Neither their drawn convergence nor their display order claims a measured docking success.

CANVAS, OCCUPANCY, AND ORIGINAL COMPOSITION

Use a 4:3 landscape white canvas, designed to remain readable when displayed at approximately 180 mm wide. Work on a notional 1000 by 750 coordinate grid, but do not print any layout coordinates. Keep all meaningful content within x=12 to 988 and y=12 to 738. The resulting crop must be tight: no unused top banner, no bottom caption band, no reserved logo area, and no large blank right corner. The word “DiffDock” is a small anchor at x=16, y=18, not a headline occupying a row. Place “Conceptual illustration” quietly at the upper right around x=830, y=18. The highest row of scientific objects begins by y=42. All large rectangles in this specification are allocation bounds, not instructions to draw large background containers.

Allocate the primary sampling-and-ranking story to x=16–668. Place a slim divider at x=684 from y=45 to y=733. The training region occupies x=700–985 and uses the short scope heading “Training only”. A vertical divider is enough to establish its scope; do not surround it with a large tinted rounded card. Keep the upper training scene above y=405 and the confidence-training scene below y=432. The small gap at y=410–430 separates two objectives and carries the corresponding subheading, not an empty strip. The left side is the major visual mass even though the right side contains technical information.

Within the left story, the compact input scene spans x=18–665 and y=47–153. Its two inputs sit on a common baseline. A recognizable ligand drawing occupies x=45–240 and a fixed protein surface occupies x=357–542. Their separate arrows reach a small initial-pose junction at x=445, y=174. Place the seed-conformer label c with the ligand and y with the protein. A light frame is permitted around each individual structural field only if it helps cropping; the fields themselves must remain much larger than their labels. The ligand is not drawn as a document and the protein is not drawn as a generic sphere or puzzle icon.

Use x=345–662 for the three central protein-conditioned pose scenes: Initial poses within y=183–281, Current pose x_t within y=309–405, and Sampled poses within y=452–550. The same protein stays fixed in orientation and scale. Put an inline Reverse update node at x=426–567, y=414–439, exactly between the current-pose field and the sampled-pose field. A short downward state arrow leaves the bottom of Current pose and enters the TOP of Reverse update. A second short downward arrow leaves the BOTTOM of Reverse update and enters the TOP of Sampled poses. Label this second short edge x_next. An initial-to-current continuation uses a short downward arrow with a small ellipsis to indicate omitted earlier reverse steps. The last field is marked t=0; the first is marked t=T. No arrow from Reverse update may extend past or around the Sampled poses field. The final ensemble is a mandatory state in the main path, never a floating illustration alongside a bypass line.

The motion explanation column occupies x=18–226 and y=190–533. Stack three compact, heavily populated molecular glyphs there, with little dead space between their visual fields. Their headings are “Translate”, “Rotate”, and “Twist”. Each shows the same identifiable ligand fragment before and after a geometric action, using dark present bonds and a light ghost only where the two states differ. Align the three headings at x=22 and align the main atom or bond centers near x=120. Keep their explanatory associations separate from the central downward sampling arrows. Draw a simple slim bracket adjacent to the three glyphs, ending near the central score-output cue; label the bracket “Pose freedoms”. This bracket groups explanations and must not have an arrowhead suggesting a serial computation.

Use x=18–665, y=568–734 for ranking. Place the main confidence operator at x=325–555, y=578–607. A single short downward data arrow starts at the BOTTOM boundary of the Sampled poses field and ends at the TOP boundary of Confidence d(x,y). This is its sole incoming pose-data edge. Add an independent short local y input at the operator’s left side. This y tab is an isolated local source: no long vertical line connects it to sampling s, its outputs, or another protein tile. Place the ranked molecular thumbnails across x=24–230, x=244–450 and x=464–664, y=634–731, with Ranked poses directly above them. d has a short downward output to this row. There is no direct Reverse update→d, x_t→d, score-model→d or training-reference→d edge in the primary sampling story. Keep the ranking row substantially filled by molecules and fixed protein crops, with little interior padding.

MOLECULAR IDENTITY AND CONCRETE SCENE

Draw a simple, trackable generic ligand rather than a complex compound with ambiguous atom inventory. Its fixed schematic molecular graph has two six-membered rings connected by a short two-carbon flexible linker. The left ring contains one visibly blue nitrogen, the right ring carries one clearly red terminal oxygen group, and all other displayed ring/linker atoms are neutral carbon. Use this same ring count, connection pattern, nitrogen position and terminal oxygen throughout the inputs, all A/B/C candidates, freedom glyphs, training examples and ranked outputs. The molecule is not assigned a real name, formula or PDB identity. Relative position, rigid orientation and linker torsions can change; rings may be foreshortened but cannot split, fuse, disappear, gain a new ring or change atom identity. Keep enough bond detail visible to inspect this invariant. Candidate outline colors do not replace atom colors.

Render the protein as a stable light-grey molecular surface with an irregular lobed contour, a broad shallow cleft, and local rounded surface detail. The cleft is a visual spatial reference, not a supplied known-pocket annotation. A fixed darker grey lobe on the upper left and a small saddle on the lower right provide orientation landmarks across repeated views. Use restrained local shading to make the geometry readable on white. Keep the protein surface larger than any single ligand, yet use enough surface transparency or local outline detail for candidate ligands to remain visible. The protein does not fold, translate, rotate independently, open a door, or change its conformation as the ligand is sampled.

Represent candidate identity with small outline tags A, B, and C near the complete ligand, using teal, amber, and violet tag colors. Use the same candidate tag color around a very thin outer silhouette or a single dot next to the molecule. Do not recolor every atom of a candidate to erase chemical atom roles. Where candidate overlap would obscure bonds, offset their projected positions slightly within the illustrative geometry and keep the tag attached with a very short leader. Do not create links between different candidate molecules. Their colored tags denote sampling branches of the same ligand, not different molecular species or interacting components.

In the initial ensemble, place A near the protein's upper outer lobe with a rotated orientation, B lower and left of the cleft with a different linker twist, and C to the right of the protein with a distinct rigid-body orientation. Keep all three visible against the same fixed surface. Their positions need not imply physical equilibrium; they represent randomized initial poses. In the middle ensemble, shift each candidate into a plausible nearby region while retaining different orientations and linker conformations. In the last ensemble, put the candidates in distinct possible placements around the cleft and its neighboring surface, sufficiently separated to inspect their geometry. Do not collapse all candidates into exactly one pose, erase two branches, or show a green success check on any position.

SCORE NETWORK AND REVERSE-STEP INTERFACE

Place the compact score call s(x_t,y,t) to the LEFT of the current central pose scene, approximately x=245–343, y=337–373. A short arrow from the current-pose field’s left boundary reaches the score block’s right-side x_t input. Give y and t two independent short input stubs on its upper side; both have visible arrowheads entering the score block. Do not connect y to t, merge their labels, or place them as unattached decorations. Below s, show the three outputs α, β and γ as vector/vector/bond-scalar glyphs at y=392. Their three short output lines enter a compact score-output collector marked (α,β,γ), then travel a short distance right into the LEFT side of the inline Reverse update node. The collector groups a score vector tuple; it does not add or average the components. Keep these lines away from protein surfaces and the central vertical state edges.

The Reverse update node at x=426–567, y=414–439 receives three clearly different inputs: the current pose from its top, the score tuple from its left, and a small sampling-noise z port near its lower-left corner. Its only output is the short x_next edge to the sampled-pose field immediately below. Place repeat to t=0 as a compact STATIC annotation beside this local state transition. Do not draw a return loop around the protein or a long vertical x_next rail reaching confidence. The final field represents the end of the repeated update sequence. It must visibly consume the update output and itself feed confidence. The state arrow never originates at the protein y input alone.

The same score model is reused across reverse steps. State this locally with the short STATIC label Shared s next to the sampling score block. In the training column, print Trained s used in sampling beside the training score objective; near the confidence-training objective print Trained d used in ranking. These are static same-model explanations with NO connecting lines, arrowheads, dotted bridges or cross-divider paths. Delete every learned s and learned d arrow from the first draft. d remains a separately trained model and does not provide a refinement feedback signal to s or to Reverse update.

THREE MOTION GLYPHS AS REAL GEOMETRIC EXPLANATIONS

The Translate glyph shows the entire ligand twice: a faint outline at a starting position and a dark corresponding ligand shifted to the right within a small fixed coordinate frame. Each atom maintains its relative position within the ligand, all rings and bond lengths remain the same, and a single straight vector connects the two centers. Place “Δr” next to the straight arrow. Keep the moving molecule distinct from the protein-conditioning surface; a small grey patch behind the pair establishes the fixed reference. This glyph explains global translation, not atom migration or molecular growth.

The Rotate glyph shows the same whole ligand with a fixed center point and two rigid orientations. Use a short axis through the center and a curved arrow around it, labeled “ΔR”. Preserve the internal linker angle in this particular pair so the rigid rotation is visually separable from torsion. A light ghost should mark only the former overall orientation. Make the center-point registration visible, with no arrow from one atom to a different atom. It is an orientation change of the same molecule relative to the fixed protein. The three-dimensional arc may use a tiny interrupted back segment to express depth, but must not look like an iterative optimization loop around all three glyphs.

The Twist glyph expands the central rotatable bond to a larger local scale. Draw its two endpoint atoms, a rigid fragment on either side, and a rotation arc specifically around that bond. Keep the upstream fragment fixed in the local explanation and show two orientations of the downstream fragment, one ghosted. Label the angle “Δθ”. The formal method disentangles the torsion update from overall rigid motion using alignment; the overview can cue this with a small “bond torsion” label, while the separately drawn translation and rotation glyphs preserve the conceptual distinction. Do not place the torsion arc through an aromatic ring bond, stretch the bond, or break and reconnect a different fragment.

CONFIDENCE RANKING AS A DISTINCT FINAL OPERATION

The final sampled ensemble supplies candidate pose–protein pairs to d(x,y). Its incoming pose edge begins on Sampled poses, not on any score/update block. Its short y port reuses the fixed protein condition and enters independently. d then feeds a compact sorting glyph or the ranked-row junction. Candidate tags are rearranged by this final ranking operation while ligand geometry is preserved from the final ensemble to the associated output thumbnail. Keep the d block flat and modest, with enough room for its exact input and output arrowheads. Do not create a second independent protein or an experimental reference at this location.

Choose the symbolic displayed order B, A, C for the output thumbnails and retain those identities exactly. Use the labels “First: B”, “Next: A”, and “Next: C”, or small ordinal positions coupled to the tags. These are illustrative ranks, not evidence that B is the true pose. The first output should be only modestly emphasized by a darker thin frame and its position in the row. Do not award it a trophy, green success shield, accuracy badge, or numeric score. The next two thumbnails remain fully visible and the same size so the method's multi-pose nature is clear. Preserve each candidate’s final geometry and the fixed protein crop in its corresponding ranked thumbnail.

SCORE-MODEL TRAINING SCENE ON THE UPPER RIGHT

Inside the right training column, use the subheading “Learn the score” at x=707, y=65. Directly below, draw a compact schematic bound complex with label “Reference pose x*”, beside a seed-conformer glyph c. Both are training-only inputs. Show a small operator “Conformer match” taking the reference pose and seed information and returning x_0, the pose on the seed-compatible manifold nearest to the reference under RMSD. Keep this training alignment visually distinct from confidence ranking and from the torsion glyph. The source uses it to respect the conformer manifold; it is not a general protein alignment module at inference time.

Below x_0 in the score-training scene, show a three-component Perturb at t operator that returns a miniature noisy pose x_t. Its three motion symbols denote Δr, ΔR and Δθ, and its small t input enters independently. The protein is not perturbed. Position the noisy training pose near x=744, y=250 and place the local training score s(x_t,y,t) at x=708–831, y=285–316. Route noisy x_t into its top port. The signature s(x_t,y,t) carries all three input identities. Add a compact static note conditioned on y, t beside this training call; do not draw separate y/t tabs or wires in the upper-right training pane. Noisy x_t has no direct connection to Score matching. This local model call is identified with the sampling s by static notation only. No training x_t, target, loss or model-output line crosses the training divider.

The perturbation operation has a second short branch to a Kernel scores group at x=850–981, y=251–315. This target group contains the analytic translation, rotation and torsion kernel-score glyphs. Below the training s block, collect its predicted α,β,γ in a short group labeled Predicted scores. Place Score matching at x=770–929, y=345–369. A short arrow from Predicted scores enters its LEFT input; a separate short arrow from Kernel scores enters its RIGHT input. The two operand groups must remain distinct until that comparison. No arrow from the loss formula or an update label points into Score matching as if it were a model parameter update. Avoid an outer bracket line joining kernel targets, predictions and the loss without visible endpoint ownership.

Below Score matching, typeset L_score = ℓ_tr + ℓ_rot + ℓ_tor and ℓ_q = ||s_q − ∇ log p_t^q||² on two readable compact lines. q ranges over the three shown freedoms. Put denoising score matching immediately beneath, and place the short STATIC statement L_score trains s next to the local model/objective pair. Do not draw any loss-update return arrow in this upper training scene. Update ownership is carried by that explicit statement and the adjacent s symbol; the only arrowed inputs of Score matching are predicted scores and kernel targets. Print Trained s used in sampling as a small static note. The loss remains a score-regression objective, not an all-atom coordinate reconstruction loss.

CONFIDENCE-MODEL TRAINING SCENE ON THE LOWER RIGHT

The trained diffusion model supplies sampled training poses to confidence training. Place Learn confidence at x=707, y=446. Below it, position a sampled-pose field x at x=708–809, y=474–540 and a reference-pose field x* at x=883–982, y=474–540. These show the same molecular identity and fixed protein crop but distinct ligand placements. Beneath and between them place ONE compact scientific target-computation box, spanning approximately x=793–969,y=555–600. Its first line is Pose error target, and its second line is b = 1[RMSD(x,x*) < ε]. Put the small definition ε: pose tolerance just beneath this box. A short branch from sampled x enters its LEFT port, and a short branch from reference x* enters its RIGHT port. This is the sole operation that produces b. There is no separate b square, no free-floating Pose error output, and no direct x*→CE or x*→b wire. The target computation represents structural error of sampled pose relative to the reference, never reference identity alone.

Place training d(x,y) at x=710–803, y=636–663 and Cross-entropy at x=847–980, y=636–663. The sampled pose x branches downward to d’s top input, and y enters d through an independent short left port. The reference x* does not enter d. A short rightward arrow carries d’s predicted confidence into the LEFT input of Cross-entropy. A short downward arrow starts at the BOTTOM of the single Pose error target box and enters the TOP of Cross-entropy. This is the b operand. No line descends from the reference image to the right of CE, and the lower-right region contains no duplicate b node. Thus the prediction and comparison-derived target are separate until CE. Keep the target-computation box compact and readable, immediately above CE, without adding an empty surrounding container.

Write L_conf = CE(d(x,y), b) directly below the Cross-entropy node, around y=688, with the STATIC statement L_conf trains d near it and Trained d used in ranking beneath. No training-update loop or cross-divider learned-d line is drawn. Training-only ownership remains explicit in the title, formula and statements. The confidence model uses sampled x and protein y; the structural comparison to x* creates b, which supervises d only in this right-hand region. Its inference counterpart on the left consumes completed sampled poses and y without access to x* or b.

EXACT EDGE MAP AND ROUTE BEHAVIOR

Use solid dark slate arrows for primary data and state transformations, including the short vertical current-pose→Reverse update→Sampled poses→d chain. Inside training, use the same readable arrow convention for data and distinct loss operands. There are NO arrowed model-update paths and NO cross-divider paths. Model training and model reuse are explicit static statements next to the corresponding s/d blocks and objectives. Use dotted unarrowed leaders only for the left motion-glyph explanations. A real branch has a small junction dot; an incidental crossing never has one. Arrowheads terminate on the precise receiving node boundary, not on protein texture, atom sticks, labels or bracket lines.

The central state lane is around x=490. Keep its three short arrows separated by their actual receiving objects: Current pose to Reverse update, Reverse update to Sampled poses, and Sampled poses to Confidence. A reader must not be able to trace an uninterrupted line from Reverse update to Confidence. The sampling s call and noise port use the left side of the inline update node; its top and bottom remain exclusive to pose-state flow. In training, s and the kernel-target group feed opposite sides of Score matching. The single Pose error target computation sits directly above the CE target port. These local alignments replace long detours. The training divider has no semantic line crossing it.

TYPOGRAPHY, PALETTE, AND OBJECT FINISH

Use a clean scientific sans-serif face, with two weights and three practical text sizes. The method name is modestly bold, the three functional subheadings are slightly smaller, and operator labels and formulas are readable technical text. Aim for effective labels around 8–9 pt and secondary notation around 7–8 pt at 180 mm display width; these are design targets, not a claim of completed print validation. Subscripts and Greek symbols remain distinct. If a formula becomes tiny, shorten nearby explanatory labels or rebalance the local region. Do not expand the main title or shrink all scientific labels to solve a layout problem.

The palette is white ground, dark slate text/arrows, neutral grey proteins, restrained standard atom colors, and teal/amber/violet candidate identity marks. A muted blue outline identifies s and a muted olive outline identifies d. Model color is separate from candidate color. There is no special color for phantom cross-scope arrows because these arrows are absent. Static training statements are dark, small and clear. Molecular surfaces may have restrained local shading; no background gradients, glossy dashboard cards, decorative stock-photo fields or broad panel shadows are used.

Print only the method name, conceptual label, scientific input/scene labels, model symbols, freedom labels, state/time notation, candidate tags/ranks, Training only, objective expressions, mask-free noise notation, and the explicitly specified static model-training/reuse statements. Do not print production headings, provenance metadata, coordinates, image-version labels, review status or a paragraph explaining the paper. Short labels identify what the neighboring molecular field or operation already shows. Do not reintroduce deleted learned-model lines merely because a static statement mentions another region.

FINAL SCIENTIFIC AND DENSITY REQUIREMENTS

Inspect semantic occupancy after repairing the routes. Preserve the tight overall visual mass of the first draft, with a short local gap around the inline Reverse update and clear minimum space for atom sticks. The lower confidence target chain should occupy its allocated column with source fields, the single Pose error target computation, prediction d, CE and its formula; no large blank target card is needed. Main and training regions remain adjacent across a slim divider. If removing the long learned-model arrows reveals a vacant strip, enlarge nearby molecular fields or tighten local spacing rather than restoring decorative lines. No broad empty corner, giant header or unused footer has a scientific function.

Render the complete revised overview from this full specification. Preserve the first draft’s useful dense molecular appearance while correcting topology locally. Inspect identical ligand graph/atom roles across all copies, independent y/t score conditioning, mandatory update→Sampled poses→d, separate predicted/kernel score operands, and Pose error target→CE. No image-version, review, measured-performance or validation text is printed. The actual output must pass a fresh visual endpoint and density review; this prompt alone does not establish a pass.
