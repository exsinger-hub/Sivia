ONE BOUNDED DENSITY EDIT of the provided DUSt3R v2 image. Preserve every correct scientific relationship and the complete lower geometric explanation. Keep both input pictures, both Joint outputs groups with pointmap plus confidence, both camera-1 superscripts, camera cues, independent P_a/P_b examples and conceptual marker. Change only the upper network's use of space: extend each real patch-token array and encoded-feature matrix vertically to occupy its image row; enlarge each decoder into a tightly populated token-transformation scene with several substantial internal token arrays. Shorten the formerly tall empty gap between decoder borders and the unnecessarily long horizontal arrow lanes. Do not satisfy this instruction by enlarging empty colored rectangles: added area must contain recognizable patch fragments, token feature cells or the decoder's actual schematic token-transformation content. No new branch, loss, architecture claim, attention heatmap, extra model or measured result. Keep the two opposite cross-view arrows readable in a narrow gap between the expanded decoder bodies. Preserve the 4:3 canvas and all bottom geometry; do not expand margins or shrink the images. The following is the full production specification.

Create a dense, source-grounded scientific concept illustration for DUSt3R: Geometric 3D Vision Made Easy, CVPR 2024. The displayed title is "Two Views, One Coordinate Frame". Directly beneath it print the exact small label "Conceptual illustration". Produce a new scene and an original overview composition, not a reproduction of the paper page, a screenshot of software, or a claimed new reconstruction result. The central claim is precise: two images are processed jointly to predict two pixel-aligned pointmaps and their confidence maps, and both pointmaps express their 3D coordinates in the coordinate frame of the first camera.

SCIENTIFIC SCOPE

Explain the pairwise network and the geometric meaning of its output. This is not a full multiview reconstruction pipeline. The source has weight-shared ViT image encoders, two transformer decoder branches exchanging information by cross-attention, and separate regression heads. Each head predicts a pointmap together with an associated confidence map for its image. There are no required camera intrinsics or extrinsics supplied as known inputs in this figure. The output geometry is subject to unknown global scale. Do not add metric rulers, known camera baselines, pose ground truth, a calibrated stereo module, or a downstream optimization network.

The key distinction from a generic stereo flowchart is visible in the two output grids. Each output preserves the pixel indexing of its own input view, while the vectors stored at those pixels live in the same first-camera 3D reference frame. The index domain and coordinate reference frame have different roles. A pointmap for the second image must not silently become a depth map in the second camera frame. The two maps can be visualized together in a common 3D scene, but that display is not an added algorithmic fusion stage.

The paired network explains joint regression; the lower scene explains the shared coordinate reference; the independent pixel inspection strips explain view-specific indexing. The enlarged token representations remain schematic, without measured activations or new architecture claims.

CANVAS AND READING SKELETON

Use a high-resolution 4:3 landscape page on white, designed for an approximately 180 mm publication width. A compact title occupies the top strip. The upper half contains two synchronized horizontal network rows. The lower half is a geometry explanation made from a large shared-frame point scene at left and two compact pixel-to-point inspection scenes at right. Keep the outer margin narrow, all content tightly arranged, and the bottom caption close to the graphics. Do not print layout ratios, coordinate instructions, dimensions of the canvas, or production notes.

In the upper rows, reserve generous area for the two input pictures and the paired pointmap/confidence fields. The encoder, decoder and head remain between the input and output carriers. Their widths remain close to the reference, but each patch carrier, encoded feature array and decoder now uses the vertical extent of its row for scientific token content. The images and output grids remain visually substantial; network content gains usable height rather than stealing their width. The lower geometric scene should occupy approximately half the usable width and nearly the whole lower height; its point surfaces, camera frusta and view-dependent samples are substantial scientific content. The lower right uses two stacked inspection strips, each with a cropped image patch, a visible pixel grid, a symbolic vector cell, and the relevant pointmap identity.

Do not make five equal empty boxes across each row. Do not center a tiny pipeline inside a large framed panel. Align both input image rectangles in the left column, both encoders in the next column, both expanded decoders in the middle column, both heads in the following slim column, and both output groups at right. The token and feature arrays fill their real row heights rather than appearing as four tiny squares surrounded by white. Their arrows are short and horizontal except the local information exchange between decoders. The lower panel boundary can be a thin rule or no-head grouping bracket. The scene itself must occupy its space, with meaningful surfaces extending toward its edges.

Use small local gutters for labels and connector tips. If an unused region remains, enlarge the associated image crop, map viewport or 3D scene. Do not add decorative robot icons, photographic cameras, computation chips, artificial metrics, or unrelated applications to occupy it. Scene texture and grid structure should be meaningful rather than decorative noise. Fine graphic detail is subordinate to the clear representation hierarchy and must remain readable when the whole figure is viewed at publication size.

PRINTED TEXT AND TYPOGRAPHY

Use a clean technical sans serif and a restrained math face for symbols. Set the title at an apparent 14–15 pt at final width, scene headings at 9–10 pt, module labels at 8–9 pt, and captions and subscripts at no less than 7.5 pt. The figure should look like a scientific illustration with crisp text, not a marketing poster. No full bibliography, affiliation, conference badge, page number, software toolbar, or watermark belongs in the artwork.

The principal labels are "Image 1", "Image 2", "Patch tokens", "ViT encoder", "Shared weights", "Decoder 1", "Decoder 2", "Cross-view attention", "Head 1", "Head 2", "Pointmap", "Confidence", and "Camera 1 coordinates". The lower scene headings are "One shared 3D frame" and "A pixel stores a 3D point". Under the title use only "Conceptual illustration". Short annotations requested later may be printed, but all explanatory paragraphs in this specification are drawing instructions rather than text to paste onto the image.

Use symbols I1 and I2 in italic math style for the two images, X^{1,1} and X^{2,1} for their pointmaps, and C1 and C2 for their confidence maps. Typeset superscripts clearly, with a comma separating the image index from the reference-frame index. The second superscript is 1 in both pointmaps. Print the compact key "X^{v,1}: pixels from view v, coordinates in camera 1" near the shared-frame scene. If full superscripts cannot be rendered legibly, print X(1→1) and X(2→1) consistently instead; do not mix both notations. Prefer the superscript notation throughout.

NEW INPUT SCENE

Both images depict the same tabletop architectural diorama. It contains one teal rectangular tower, a mustard three-step block, and a small coral triangular-roof block arranged on a pale gray base plate. The teal tower stands toward the rear left of the base. The mustard steps sit forward and slightly right, with their risers facing the first camera. The coral roof block is farther right, separated from the steps by a visible narrow gap. A thin dark seam runs across the gray base plate behind the steps. These simple, colored planes make orientation, visibility and correspondence easy to see without copying the colored cube from the source figure.

The teal tower has a flat top, one broad front face and a narrower side face visible from the first camera. Add two small vertical recessed grooves on its broad face, enough texture to distinguish orientation without introducing text. The mustard block has three actual ascending horizontal treads and vertical risers, not a painted staircase pattern. The coral roof has two sloped surfaces meeting at a ridge. The base plate has a visible thickness and casts a soft contact shadow on the table. Keep all these features consistent across both input views and the conceptual 3D display.

Image 1 is an oblique view from front left, looking slightly down at the scene. Image 2 is taken farther to the right around the same objects, with a modestly changed viewpoint and perspective. Both images show a useful overlapping part of the diorama. The tower partly occludes a different patch of the base plate in each view. The second view exposes more of the roof block's right sloping surface and a different side of the steps. These visibility differences make the pair meaningful; do not duplicate Image 1 and merely tint it blue.

Keep the photographic fields closely cropped around the diorama. Do not include a wide blank wall, empty horizon, human hands, rulers, sensor apparatus, dense background objects, or a real branded toy. Use realistic but restrained material shading, stable colors, and subtle contact shadows. The table and base remain distinguishable. The images are illustrative synthetic views and are not presented as benchmark samples. A small shared caption below the left image column reads "Same scene, different viewpoints".

INPUT INDEX MARKERS

On the top tread's front corner of the mustard steps, place one tiny circular marker a in Image 1. Place marker b on a visible staircase surface location in Image 2. The two chosen pixels are independent examples, not a verified matching pair. Use a thin dark ring with a small white label backing so the markers remain readable without obscuring the corner geometry. These labels name two view-specific sample pixels. They are not object identities, camera labels, or network branches. The lower shared-frame frusta indicate viewpoints only; do not force their rays to establish correspondence between a and b. Each inspection strip has its own point P_a or P_b.

Only these two selected pixels need visible markers in the input images. Do not cover the whole scene with correspondence lines. A larger crop of each marked corner appears in the lower right inspection strips. Retain the camera view and visible face orientation within each crop. This crop is a magnification of a region of its own input, not another image from a third viewpoint. Use a no-head crop leader or a local matching label to associate the crop with its source; do not draw a long diagonal computational arrow across the full figure.

PATCH TOKENS AND SHARED ENCODING

Immediately to the right of each image, draw a substantial patch-token array closely registered to that row. Preserve its existing narrow horizontal slot, but expand its height to nearly the full input-picture height. Use three closely packed columns and several rows of square image-patch fragments; the displayed count is a schematic sampling of image tokens, not an architecture dimension. Each cell contains a recognizable crop of teal tower, mustard steps, coral roof, gray base or a boundary between surfaces. Those image fragments are scientific token content. Do not fill cells with an unrelated decorative pattern, repeat a single flat tint across the entire array, or show only a tiny four-cell strip. Keep the local label Patch tokens above the array and a small ellipsis near its end. The input's short rightward arrow enters the token array at its mid-height; another short arrow goes from the array's right edge into its own encoder. A corner grid on each image still illustrates patchification without hiding the scene.

The two ViT encoders retain matching architecture and shared parameters. Their bodies can become slightly taller within their current columns so each contains the readable name ViT encoder and a substantial internal array of encoded-token cells. Put the name in the upper portion and a compact matrix of multichannel token glyphs below it. The glyphs express that the encoder processes image tokens, not a new attention-map output or a claimed layer/patch count. Use small but legible individual cells with muted teal, blue, gray and ochre feature channels. Do not invent numeric activations, self-attention probabilities, learned correspondences or extra outputs. A no-head dashed link connects the two actual encoder borders in their intervening narrow gap and is labeled Shared weights. Its endpoints stay on encoders only. Do not enlarge the dashed-link corridor, and do not let the label collide with token cells.

Each encoder produces its own feature carrier F1 or F2 immediately at the right boundary. Replace the formerly tiny single column with a tightly packed rectangular matrix of encoded feature tokens, several cells wide and several cells tall, filling the available height between the label and row boundary. A cell is a symbolic feature token, not a probability or a measured activation. Keep the matrix narrower than the encoder and far narrower than the decoder. The arrow from the encoder enters the matrix's left midpoint, and the arrow from that matrix's right midpoint enters the corresponding decoder's left boundary. The matrix and label remain distinct from the decoder interior. F1 and F2 never merge, pool together or swap branches.

Use the observed 1448×1086 reference proportions as construction guidance only, never printed text. In the top row keep the source image near x=22..334,y=99..278; enlarge the patch array within approximately x=358..424,y=104..270; the encoder occupies roughly x=447..554,y=103..270; and F1 occupies x=566..625,y=124..250. Decoder 1 extends to roughly x=641..844,y=105..263, with its label near the top and meaningful token content below. In the bottom row retain the image near x=22..334,y=323..498; use the same column arrangement, with patch array y=330..496, encoder y=329..496, F2 y=350..476 and Decoder 2 y=334..494. These are approximate edit targets, not exact dimensions or model settings. Keep necessary arrows short in the existing gaps. The upper network gains content within previously unused space, without moving the boundary above the lower half or stretching the canvas.

DECODER INTERACTION

Draw Decoder 1 and Decoder 2 as paired lavender operator rectangles, one per row, expanded vertically within their existing columns. Each has a shallow name line and a densely populated internal token-transformation illustration. Below the name draw two substantial token matrices side by side, each several cells wide and high, joined by one short rightward arrow. Use a small stack offset or a compact ellipsis to indicate repeated transformer processing without asserting an exact layer count. The cells are recognizable discrete latent-token carriers with small channel bands; they are not a measured attention heatmap. The left matrix denotes an intermediate branch representation and the right matrix denotes the branch's updated representation. Do not add scientific labels for unverified submodules, separate Q/K/V equations, new normalization blocks or recurrent state. This enlargement makes the originally tiny repeated token-grid motif legible; it does not alter the source architecture.

Use most of each decoder's interior for the two token matrices: for example, the top arrays can occupy about y=145..239, with only a narrow arrow gap between them, and the lower arrays can occupy about y=374..469. Keep a small padding around the operator border and no large unused lavender field beneath the token arrays. The decoder name remains distinct from its contents, and the outgoing head arrow starts on the actual right border at row center. Both branches retain their identities and feed their separate existing regression heads.

Between the expanded decoder borders retain exactly two short parallel vertical arrows. One starts on the BOTTOM boundary of Decoder 1 and ends with one arrowhead on the TOP boundary of Decoder 2. The other starts on the TOP boundary of Decoder 2 and ends with one arrowhead on the BOTTOM boundary of Decoder 1. Keep their shafts horizontally separated, with the compact Cross-view attention label immediately beside them. The source-to-target gap is now much shorter because the decoder interiors use the previously blank space. Do not replace the two directions with one undirected line, a broad loop or a model-to-image feedback path. Avoid any arrowhead hidden under a label or inside a token cell.

The exchange depicts that a branch attends to features from the other view during decoding. It does not mean that the output pointmap of one branch is fed into the input image of the other, or that one view is reconstructed first and used as a fixed teacher. Both branches process the pair jointly. No external text prompt, epipolar-matching block, correspondence solver, pose-estimation module, or calibrated triangulation module should be inserted between them. The source's learned pairwise regression remains the central computation.

HEADS AND OUTPUT CARRIERS

The top decoder sends one short rightward arrow into Head 1. The bottom decoder sends one short rightward arrow into Head 2. Use small charcoal-outlined regression-head bars. After each head, draw exactly one short arrow into one tightly enclosing joint-output group that contains both its Pointmap field and its Confidence field. Add the small group label Joint outputs. Erase the old duplicated arrows that both enter the pointmap. The single head arrow ends on the joint group left boundary, aligned with the head center. The group is only a representation carrier, not a new operation. Both map and confidence are inside its light border and are jointly produced by that head. There is no arrow from pointmap to confidence, no second dangling branch, and no confidence field outside its corresponding output group.

The top output group has heading "Pointmap X^{1,1}" and a separate small field headed "Confidence C1". The bottom group has "Pointmap X^{2,1}" and "Confidence C2". Make each pointmap field roughly twice the area of its adjacent confidence field. The pointmap fields are pixel-grid representations with spatially coherent colored regions corresponding to the same diorama seen in their respective input views. Use clearly schematic coordinate colors or finely sampled colored dots within the image-aligned grids. A small caption under each pointmap reads "H × W × 3"; under each confidence map print "H × W".

These dimensions explain representation shape, not a measured resolution. Do not print an actual pixel resolution, frame rate, memory use or accuracy. A pointmap cell stores a 3D vector, so the three-channel dimension cannot be replaced by a single depth channel. The map for Image 2 retains Image 2's image-plane arrangement even though its vector values use Camera 1's axes. Consequently the top and bottom maps need not have identical silhouettes or visible faces. Their physical surfaces correspond, while their pixel layouts differ with viewpoint.

For the confidence carrier use a small monochrome patterned image-plane grid with gentle spatial variation and the word "schematic" underneath. Avoid a numerical colorbar or labels such as 0.98, high accuracy, error-free, guaranteed match, or calibrated uncertainty. The source predicts confidence, but this generated field is not a measured network response and should not encode a fabricated quantitative conclusion. The varying pattern simply shows that confidence is spatially associated with the pointmap. It is not a segmentation mask or a physical surface texture.

PLACE BOTH MAPS IN CAMERA 1 COORDINATES

Below the two network rows, draw a substantial 3D scene headed "One shared 3D frame". This scene is a visualization of the two output point sets. It is not an additional network module. A short red display leader from X^{1,1} and a short blue display leader from X^{2,1} may approach the top border of this scene, each ending on its own color key. Use a no-head display leader or a small matching map label if long arrows would cross the network. Do not create an unlabeled computational merge node before the scene.

Within the scene, redraw the diorama as a dense but readable collection of sampled surface points. Include the teal tower's vertical and top planes, the mustard stepped horizontal and vertical planes, the coral roof's slopes, and the gray base plate. Use a faint neutral wireframe underneath to clarify surface geometry. Overlay red samples from X^{1,1} and blue samples from X^{2,1}. Where both views observe a surface, interleave the two colors closely, so a shared surface is recognizable without claiming numerical equality. Where one view reveals a side unseen in the other, show only the corresponding color's samples.

The red and blue samples must occupy one common geometric arrangement. Do not place two independent floating reconstructed objects in separate coordinate frames. Do not turn the red samples into a red solid object and blue samples into a blue duplicate object separated in space. The colored point layers represent different view-indexed observations of the same conceptual diorama. Individual point spacing can be regular enough to be legible, but exact sample count has no scientific significance. Avoid isolated random dots outside surfaces that would look like an error distribution or a star field.

Draw one coordinate triad at the first camera's origin, with thin charcoal x1, y1, z1 axes. Label it "Camera 1 at origin". A small red outlined camera frustum extends from that origin toward the scene. Use one convention consistently: the z1 axis points forward from the camera toward the diorama, x1 extends to the camera's right, and y1 completes a coherent right-handed local frame. The diagram is schematic and the axes carry no numerical ticks. Do not add meters, centimeters, a calibrated baseline, or a scale bar.

A small blue outlined frustum placed to the side illustrates Camera 2 viewing the same scene. Its location is a schematic viewpoint cue, not a known input pose. Label it "Camera 2: pose not supplied". Do not draw an independent x2/y2/z2 triad around it because the output is expressed in Camera 1 coordinates. Do not draw a pose vector leaving Head 2. The frustum only makes the difference between viewing location and coordinate reference understandable. Keep both frusta small relative to the point surfaces, with enough space to see their orientation.

Add the exact short annotation "Unknown global scale" beside the shared scene, away from the axis labels. This is a method condition, not an uncertainty score on any individual point. Another small phrase may read "Both maps use camera 1 coordinates". Keep the key near the points it describes. Do not add a scale-normalization block, numerical transformation matrix, homography, triangulation equation, or a claim that the architecture explicitly enforces physical camera geometry. The network learns these outputs from training; this overview concerns their representation and shared frame.

PIXEL-TO-POINT INSPECTION STRIPS

At the lower right place two dense horizontal inspection strips. The upper is labelled "View 1 pixel a" and the lower "View 2 pixel b". Each begins with a magnified crop of the marked step corner from its own image. Overlay a small square pixel grid on the crop, and outline one grid cell using its branch color. The cell in View 1 is red; the cell in View 2 is blue. Use a visible center dot rather than a large filled circle that would hide the actual edge geometry.

Next in each strip show a symbolic pointmap cell, drawn as one compact three-slot vector carrier. In the upper print "X^{1,1}[a] = (x_a, y_a, z_a)"; in the lower print "X^{2,1}[b] = (x_b, y_b, z_b)". These are symbolic vector forms, not numerical values, so no arithmetic equality between two measured predictions is asserted. Under each vector print "3D position in camera 1 frame". A short single-headed arrow leaves the outlined pixel and enters this vector cell, showing the per-pixel storage relationship. Keep that arrow confined to its inspection strip.

At the far right of each strip, show a small isometric detail of the same step corner with a colored point on its surface. This is a local geometric carrier rather than another whole reconstruction scene. Label the top point "P_a from view 1" and the lower point "P_b from view 2". A short arrow leaves each symbolic vector cell and touches the surface point's small carrier boundary. Do not draw a vector arrow from the image-plane cell directly into the 3D scene across the page. The local strip explains the mapping while the large scene explains the common frame.

The two point labels refer to independent illustrative pixels and their associated surface locations. They are not claimed to be a correspondence pair. Add a tiny shared note under the strips: "Independent sample pixels; no measured coordinates". Do not print fabricated XYZ triples, pixel coordinates, Euclidean errors or matching confidence. Each crop must match the local surface detail within its own strip. The two strips may show different staircase surface points, as indicated by P_a and P_b; do not label them with a shared P.

Add a small no-head bracket under the two vector cells with the phrase "Different pixel grids, shared coordinate reference". This bracket expresses the scientific distinction and is not a data flow between the two outputs. The two strips remain independent. No arrow points from the upper pointmap cell into the lower one, and there is no claim that pixels a and b have the same image-plane index. Their different labels deliberately preserve the different index domains.

VISIBILITY AND GEOMETRY DISCIPLINE

Ensure the novel input views, pointmap layouts and shared surface scene are mutually compatible at the conceptual level. The mustard staircase has the same number of steps in every view. The tower does not switch sides relative to the coral roof. The base plate seam changes perspective but not physical location. Surfaces newly revealed by the second viewpoint are not accidentally shown as fully visible in the first image. These constraints make the scientific example inspectable without elevating it to a generated experimental result.

The confidence maps need not mirror geometry colors; they are separately patterned scalar carriers. The pointmaps should not be styled as segmentation masks with class names. The large red/blue point display uses branch identity colors, whereas the input images use natural object colors. Distinguish these two color systems in a compact legend: a red dot labelled "Samples from X^{1,1}" and a blue dot labelled "Samples from X^{2,1}". Place this legend at the edge of the point scene, not in a large empty legend card.

Keep all geometric relations explanatory. Do not add a ground-truth mesh, before/after improvement panel, target-versus-prediction error heatmap, mesh-completion claim, or success badge. Do not invent a guarantee of perfectly aligned predictions; the common reference frame is the representation contract, while the drawn overlap is a conceptual instance. No benchmark dataset name or quantitative performance appears. If the geometry becomes visually complex, reduce ornamental surface texture rather than replacing the scene with a generic box labelled 3D.

LINE LANGUAGE AND LAYERING

Use charcoal outlines and single arrowheads for the main image-to-network and head-to-output paths. Use muted lavender for decoder operations and subtle neutral fill for encoders and heads. Branch 1's small identifiers and geometry samples are red; branch 2's are blue. Weight sharing and geometric display associations are dashed or no-head lines with their own labels. Do not use the same line treatment for a computational edge and a crop/expansion association. An arrowhead always ends on an actual receiving object boundary.

Draw image and map fields first, then grid overlays and sample markers, then operations, connectors, and labels. Camera axes and frusta belong behind or beside the point surfaces, not on top of critical sample markers. Fine wireframe edges are lighter than the colored sampled points. Keep arrow shafts clear of mathematical superscripts. No line may cut through a confidence field simply because it is on the way to another destination. Short bends are permitted only in reserved gutters, with no unlabeled crossings or floating endpoints.

Maintain a consistent stroke hierarchy: solid main arrows strongest; object boundaries medium; pixel grids, point surfaces and associations lighter. Use flat editorial vector forms outside the concept image viewports. Avoid beveled boxes, broad shadows, glossy model icons, neon glows, ornamental circuit paths or artificial paper texture. Gentle shading inside the physical diorama serves shape interpretation and is welcome. Model operators use tight outlines and actual internal token content. Their enlarged scientific representations reclaim unused space while the input images, pointmaps and lower geometric explanation remain large.

FINAL CONTENT CHECK

Follow both rows from the correct image through its own patch tokens, shared encoder instance, corresponding decoder and separate head. Confirm the shared-weights label only joins the encoders. Confirm two opposite single-headed cross-view arrows join the decoders locally. Confirm each head feeds one explicitly bounded Joint outputs carrier containing both a three-channel pointmap and its scalar confidence map. No duplicated arrows both enter the pointmap, and confidence is visibly part of the same head output group. Read both pointmap superscripts and verify that their second index is 1. Confirm the confidence fields are marked schematic and contain no invented numerical calibration.

Inspect the lower scene as geometry rather than decoration. One coordinate triad belongs to Camera 1, both sampled layers lie on the same conceptual scene, Camera 2 is a viewpoint cue whose pose is not supplied, and unknown global scale is printed. Inspect each magnified pixel example for its own correct local staircase surface and per-view index; P_a and P_b are independent illustrative samples. The index domain must not be confused with the shared coordinate frame. No global alignment optimizer, known pose input, or measured reconstruction accuracy has been introduced.

The finished raster should be rich in inspectable objects, patches, point grids and local geometric correspondence. Keep the title modest, all explanatory graphics close to their carriers, and the final footer short. There should be no large vacant container, broad unused band or unrelated module. Render only the finished scientific illustration with its visible conceptual marker. Do not print this prompt, a checklist, an admission decision, validation claims or production commentary.
