FINAL BOUNDED LOCAL EDIT of the provided iBOT v2 figure. Keep the bird scenes, image crops, dense layout, correct augmentation branches, bottom cross-view losses, G gradient ports, EMA and learned features. Simplify ONLY the four token-output interfaces and same-view patch glyphs. In EACH model call put the PATCH branch in the UPPER lane aligned with its patch-output field. Its h→P arrow feeds that patch field directly. Put the singleton CLS branch in the LOWER lane, terminating at its C badge; the C badge has NO outgoing rightward wire. Delete the four existing CLS→patch-strip arrows and the intervening CLS text beside each strip. Keep the exact C_ut/C_us/C_vt/C_vs identities for continuation to the unchanged bottom class comparisons. To make position correspondence indisputable, replace each long row of tiny patch squares with TWO larger selected-position distribution glyphs, with ellipses outside them. Upper-view glyphs are u:i and u:j; lower-view glyphs are v:k and v:l. Each teacher/student pair has these labels at exactly matching horizontal centers. Teacher glyphs contain dark soft-target bars; student glyphs contain blue prediction bars, NOT slash-filled output tokens. Retain masking only in input image cells. These paired selected-position glyphs feed the existing Patch CE operands. The complete self-contained production specification follows.

Create a complete revised iBOT pretraining METHOD OVERVIEW using the supplied first draft as the composition reference. Preserve its dense bird/crop imagery, two aligned view rows, four compact teacher/student calls, same-view patch comparisons, lower cross-view class losses, update/EMA region and tight 3:2 white canvas. Repair only the scientific output branching, view distinction, mask correspondences, gradient origin and overly specific learned-output label described in this full prompt. This is a full overview specification, not a delta. Keep the small iBOT, Pretraining and Conceptual illustration labels; do not add a banner, large empty region or extra algorithm stage.

SCIENTIFIC ARGUMENT AND OVERVIEW SCOPE

The source authority is iBOT, arXiv 2111.07832v3, ICLR 2022, specifically Sections 3.1–3.2 on pages 3–4, Figure 3 on page 4, and the two-view pretraining pseudocode on page 13. The single visual message is that an EMA teacher provides online soft token targets, while the student learns both global agreement across augmented views and patch semantics at masked positions within the same view. The teacher is not a frozen pretrained external tokenizer. Its parameters follow the student by exponential moving average. Teacher targets are detached from the gradient. The student and teacher have the same architecture but distinct parameter values; within each network, the class-token and patch-token projection heads share parameters.

This figure covers the self-contained two-view core framework shown in the source Figure 3 and Appendix A pseudocode, not every optional crop or downstream evaluation protocol. Both image views u and v are passed unmasked to the teacher and in masked form to the student. Class-token self-distillation pairs the teacher output for v with the student output for masked u, and pairs the teacher output for u with the student output for masked v. Masked-image-modeling supervision pairs teacher and student patch tokens from the same view and same spatial position, and its loss is restricted to the masked positions. Do not exchange these pairings for a generic agreement loss.

CANVAS, REGIONS, AND DENSITY

Use the supplied 3:2 first-draft canvas and preserve its existing region positions, proportions, object scale, bird scene, palette and type hierarchy. The notional 1200×800 allocations below describe those existing zones; they do not authorize a new global layout. Keep meaningful content near the existing narrow margins, with no added footer or unused title area. Target review width remains approximately 180 mm, with readable short operator labels and secondary notation. Do not shrink all text to fit the repairs. Local port and mask changes may shift nearby glyphs slightly, but the original dense overall composition is locked.

The input and patch-example column occupies x=18–230, y=62–594. The two main view rows occupy x=258–1182, with row u at y=65–295 and row v at y=359–589. The lower cross-view and objective area occupies x=18–1182, y=624–787. A narrow gap between the view rows carries only a compact shared-network note and the pair of parameter identities; it is not a full-width empty lane. Each region boundary is an invisible allocation, not an instruction to draw a background card. Use a faint separator line between the rows only where it aids grouping. Images, token sequences, probability glyphs, and explicit comparison operands provide the density.

Place the original example image in x=22–222, y=72–264. Directly beneath, use the space x=22–228, y=301–586 for a compact magnified patch-and-mask explanation. This is an explanatory inset associated with the same image, not a third training view. It shows one spatial patch becoming a mask embedding for the student while the teacher observes the original patch. There should be no large vacant lower-left corner. The original image and the patch inset together balance the two populated rows on the right without competing with the main teacher/student correspondences.

At the bottom, allocate x=24–742 to the cross-view class-token comparisons. Use two small, side-by-side comparison groups with visible teacher-target and student-prediction ports. Allocate x=778–984 to loss combination and the student update. Allocate x=1012–1180 to the compact learned-representation endpoint. These areas are adjacent and tightly populated, without broad background panels. A short matching-port continuation carries each upper-row class token to the appropriate lower comparison, so the lower region does not need a bundle of diagonal lines crossing the patch-token fields. The viewer must be able to read the exact token identity at both ends of every continuation.

CONCRETE IMAGE AND MATCHING VIEW GEOMETRY

Draw one original natural-image illustration of a small blue-grey songbird perched on a diagonal brown branch, with a yellow-green leafy background. The bird has a dark eye, pale cheek, short pointed beak, textured wing feathers, and visible feet gripping the branch. This is a newly drawn illustrative scene, not a photograph copied from the source paper, not an ImageNet example with a claimed identity, and not empirical output from iBOT. Keep the scene recognizably the same wherever it appears. Do not invent a class label, dataset index, species name, ground-truth segmentation mask, or annotation box as supervision.

The original source-image field is approximately square and includes the whole bird, the nearby branch fork, and background leaves. Draw two small crop outlines over this field: u includes the bird's head, shoulder, and branch, while v is shifted and slightly wider, including the wing, torso, and more background. Keep both crops plausible augmentations of this same image. A teal outline identifies view u and an amber outline identifies view v. These colors indicate view identity only. Teacher and student identity use neutral grey and blue model accents, so the figure does not confuse a view with a network.

Draw view u as a landscape crop in x=274–418, y=76–164. Draw its masked student counterpart in x=274–418, y=190–278. The two fields must have identical crop extent, orientation, scaling, patch grid, and underlying visual content. Only the selected student patch positions are replaced by mask-token tiles. A short downward arrow from the unmasked u field to the masked counterpart indicates “Mask”. The unmasked field also feeds the teacher directly from its right edge. The downward mask edge is not a path through the teacher. The student input is named û, with a clear hat on u where practical.

Draw view v in x=274–418, y=370–458 and its masked student counterpart in x=274–418, y=484–572. Apply the same correspondence rule: v and v̂ share an identical crop and patch coordinates, while u and v remain visibly different augmentations. A different mask pattern on v̂ can reveal that masks are view-specific. Label this row “View v” in a small amber accent. Do not create a pixel-to-pixel correspondence claim between patch i in u and patch i in v merely because the display grids use similar layouts. Patch indices are local to a view. Only the class-token target crosses views.

Keep the existing schematic six-column by three-row crop grids in the four teacher/student image fields; their drawing resolution is not an actual iBOT architecture setting. Make the teacher and student grids identical within each view. For this conceptual illustration, show exactly two masked cells in each student image: in view u, the adjacent cells in row 2, columns 3 and 4, labeled u:i and u:j; in view v, the adjacent cells in row 3, columns 2 and 3, labeled v:k and v:l. These are local layout instructions, not numbers to print or a claim about training mask ratios. Remove every extra mask slash in those crop fields. The selected cells become opaque pale mask tiles for the student, with no original image texture visible through them. The teacher retains the original texture at the same cells. Tiny local i/j or k/l position cues follow the relevant cell without implying correspondence between u and v.

INPUT BRANCHES AND THE LOCAL MASK INSET

At the right edge of the original bird image, retain Augment and two separate one-way branches: one branch produces the teal u crop; the other produces the amber v crop. Keep the existing short branch lanes and their u/v tags. Delete the vertical double-headed same-signal line and its words between u and v. No connector of any style joins u directly to v, or v to u. They are different augmentations of the same source image, not identical signals. The u branch reaches the unmasked upper crop, and the v branch reaches the unmasked lower crop; each crop separately branches to its own teacher and its own local Mask operation. The masked crops are not intermediary sources of the unmasked teacher crops.

Retain the populated lower-left patch example inset in its existing size. Bind it explicitly to u:i, the selected row-2/column-3 patch of view u. Its teacher branch shows the original texture and a soft token target; its student branch shows that same position replaced by e_MASK and a soft prediction. The teacher and student patch identities remain identical, but their observed image content differs. A short replacement arrow belongs only to the student mask operation. Do not duplicate the inset into a third crop or connect its original pixels directly to the student. Keep the existing original/mask image pair and small target/prediction distribution glyphs; adjust their texture crop locally only as needed to match u:i.

Immediately below those two tiny token glyphs, draw two soft categorical-distribution strips with several vertical components of unequal heights. The components are symbolic probability categories, not named object classes. Use one neutral target strip q_t and one blue prediction strip p_s, aligned so the same component positions can be compared. Include no numeric values, accuracy bars, or measured similarities. Mark the comparison “Soft target”, not “bird”, “beak”, or a token ID asserted as truth. These illustrative bars explain a probability distribution and must not appear as an experimental plot with axes, calibration statistics, or a success trajectory.

TEACHER AND STUDENT CALLS IN EACH VIEW ROW

In row u, place the teacher call at x=457–590, y=90–145 and the student call at x=457–590, y=204–259. In row v, repeat the teacher call at x=457–590, y=384–439 and the student call at x=457–590, y=498–553. The teacher calls use the exact label “Teacher f_t → h_t” and the student calls use “Student f_s → h_s”, allowing two short lines. These are repeated invocations of the same teacher and the same student, not four separate parameter sets. Add small matching θ_t and θ_s tags on the corresponding blocks; a brief centered note between rows reads “Same networks across views”.

Inside each of the four model calls, retain the backbone f followed by ONE shared projection head h. Draw a clear local fork at h. The UPPER branch outputs patch distributions through a P badge aligned with the neighboring patch-distribution field. Its short rightward arrow enters that field directly, with no CLS symbol on the route. The LOWER branch produces a singleton class distribution and terminates at its closed C badge. This lower C has no outgoing arrow to its right and does not continue upward into patch outputs. A shared h label remains inside each network because the same head parameters process both token roles. The patch and class branches are independent outputs of h; neither token role is an input to the other. The upper P and lower C ordering is consistent across all four calls.

Teacher output preparation applies to both token roles, with separate class and patch normalization scopes. On each teacher’s class branch, indicate centered/sharpened detached targets before the terminal C port. On its patch branch, retain a compact center + sharpen and stop grad cue before the terminal P port. The two cues can share a small static explanatory brace saying targets: center + sharpen; stop grad, but the class and patch output arrows stay distinct. The brace does not assert equal centers or temperatures. Every teacher target reaching either class CE or patch CE has passed a stop-gradient boundary. Student outputs do not pass through that stop-gradient mark, and EMA is not stopped by it.

Student h_s likewise branches into a singleton class prediction and an ordered patch prediction group. Each branch has its own softmax/output cue and ends in a separate C or P port. In the upper student call, use C_us for the masked-u CLS prediction and P_us for masked-u patch predictions. In the lower student call, use C_vs and P_vs for the masked-v counterparts. Teacher ports are C_ut/P_ut and C_vt/P_vt. Port names are interface identities, not extra operators. Each lower C port is capped and has NO outgoing rightward arrow toward any patch-output field. Patch fields receive only matching upper P ports. Keeping a small physical gap between the lower C and upper P terminals makes their distinction visible without expanding the model blocks.

SAME-VIEW PATCH SUPERVISION AS THE DOMINANT LOCAL RELATION

Retain the upper same-view patch comparison region, with a teacher target field and a student prediction field on aligned baselines. A short local arrow starts at the upper P_ut badge and enters the teacher field, and a separate short arrow starts at P_us and enters the student field. There is no C_ut or C_us arrow here. Replace each full tiny-square strip with only TWO selected-position distribution glyphs: one tagged u:i and the other tagged u:j. Put small ellipses before and after the pair to indicate omitted positions. These two glyphs are not adjacent full-sequence indices: they are the two displayed supervised positions from the selected mask set. Teacher and student u:i glyphs align vertically; teacher and student u:j glyphs align vertically. Each teacher glyph is a compact dark four-bar symbolic soft-target distribution; each student glyph is a compact blue four-bar symbolic prediction distribution. Heights are conceptual and carry no axis, numeric probability, measured activation, or confidence label. No student OUTPUT glyph is filled with a mask slash: masking belongs to its corresponding input image patch.

Keep Patch CE in the upper row near its current right-hand position. The two selected teacher-u soft targets reach its target input through one short arrow labeled q_t(u_i), with stop grad at the target side. The two corresponding student-u predictions reach its separate prediction input through a short arrow labeled p_s(û_i). Each arrow begins at the appropriate distribution-field boundary, not a floating label. A small unarrowed brace below the student pair marks i ∈ M_u. The u:i and u:j identities match the two masked cells in the student input image and unmasked cells in the teacher input image. The brace governs which patch positions contribute to CE; it does not merge target and prediction into one tensor.

Mirror this local selected-position display in view v. Its upper P_vt and P_vs badges supply separate teacher/student fields. Each contains exactly two selected-position glyphs, tagged v:k and v:l, with matching horizontal centers across the teacher/student pair. Teacher targets are dark symbolic distributions and student predictions are blue symbolic distributions. Place small ellipses outside the pair to indicate omitted positions. Use amber tag outlines to distinguish view v, without implying that v:k corresponds spatially to u:i. Its mask-set brace is i ∈ M_v. Both exact same-view operands enter the lower Patch CE to produce L_v. The image cells and distribution tags refer to the same two selected masked positions within each view; no patch-target edge crosses views.

For each row's compact objective, the printed text can be “L_u = mean masked CE(q_t(u_i), p_s(û_i))” and “L_v = mean masked CE(q_t(v_i), p_s(v̂_i))”, respectively. Put each on two balanced lines if required. CE takes the teacher target first and the student prediction second. The nearby mask-set bracket defines the averaging scope and the small “same view, same position” label names the illustrated alignment. These are conceptual notation choices consistent with the source's masked-position sum and normalized pseudocode. The loss does not reconstruct RGB pixels and does not require an offline one-hot token ID.

CROSS-VIEW CLASS-TOKEN MATCHING WITHOUT LONG DIAGONALS

All four CLS outputs terminate in compact, closed continuation ports beside their source model calls. Use C_ut with u_t CLS, C_us with û_s CLS, C_vt with v_t CLS, and C_vs with v̂_s CLS. Put each C identity on the singleton class-token output, separated from its neighboring P patch-output identity. The lower class-branch source C ports are terminal endpoints, not transit boxes on a line to the patch strip. In the lower cross-view region, repeat the corresponding C identities next to their existing class-distribution glyphs. Matching C names carry the same class signal from its source to that comparison. No full-height class arrow is drawn, and no CLS-to-patch edge exists anywhere.

Preserve the two lower cross-view CE groups and their correct operand order. In the left group, the target port C_vt supplies v_t CLS and the prediction port C_us supplies û_s CLS; they enter Cross-view CE on separate short lanes and produce C_u. In the right group, target C_ut supplies u_t CLS and prediction C_vs supplies v̂_s CLS, producing C_v. The teacher operands retain stop grad; student operands remain predictions. C_ut/C_us/C_vt/C_vs are tensor continuation-port IDs, while C_u/C_v are loss-term names, and their distinct typography/placement prevents conflation. Do not create a direct within-view teacher/student CLS comparison or a bidirectional u-v image edge.

Use the compact definitions “C_u = CE(q_t(v), p_s(û))” and “C_v = CE(q_t(u), p_s(v̂))”. Here q_t and p_s in the class comparison refer specifically to class-token distributions; the printed CLS ports establish that scope. Connect C_u and C_v to a small average node labeled “L_CLS”. The order of the target/prediction operands must remain exact. This class objective expresses cross-view global agreement. It is not a spatial alignment loss, patch correspondence loss, supervised bird-category loss, or proof that all feature values in the two views are already identical.

LOSS COMBINATION, UPDATE OWNERSHIP, AND LEARNED OUTPUT

Preserve L_u and L_v entering their compact average L_MIM = (L_u + L_v)/2. Preserve the two cross-view class terms entering L_CLS = (C_u + C_v)/2. Those two averages remain the separate operands of the visible total-loss node L = L_CLS + L_MIM. At the RIGHT boundary of this actual total-loss node, draw a short arrow to a small output continuation port G, explicitly labeled gradient of L. The G connection starts on the total-loss node itself; there is no floating origin. This port carries the student-training gradient relation, not an additional loss, trainable model, predicted token or image input.

Retain the local θ_s and θ_t parameter blocks in the existing Update and EMA region. Place a matching G continuation port immediately to the LEFT of θ_s. A short horizontal arrow goes from that G port into the LEFT edge of θ_s, with gradient update (student only) directly above it. Delete the first draft’s free vertical gradient-arrow start in whitespace. The two G ports establish the explicit total L→student update dependency without a long crossing line. Preserve the short θ_s→θ_t arrow labeled EMA, and keep θ_t ← m θ_t + (1−m) θ_s directly below it. The EMA arrow starts on θ_s and ends on θ_t; no loss-gradient arrow enters θ_t. m is a symbolic momentum with no invented value.

Keep the matching θ_s and θ_t parameter-use tags on the repeated model calls above. They identify the same student and the same teacher across both views, without a new parameter set for each call. Use local labels or small static sharing braces rather than long vertical weight buses. The complete update ownership is now shown by the two matching G ports and the local θ_s→θ_t EMA arrow. Teacher targets are detached for backpropagation, while teacher parameters still change through EMA. The bottom explanatory note about teacher stop-gradient/EMA can remain in its current compact position, with no arrows from that note pretending to update a model.

In the existing far-right outcome area, replace the heading Pretrained encoder with Learned representation. Remove the f_s-specific encoder wedge and any student-only deployment label. Keep a populated symbolic global-feature vector and spatial patch-feature grid, with the short caption after pretraining. These are conceptual representations, not generated activations or evaluation results. Do not replace f_s with an equally unconditional f_t claim: the official implementation supports choosing teacher or student checkpoints, and this general method overview does not select a downstream recipe. This outcome is a static summary of learned visual features and has no arrow from the loss, masks or EMA target computations as if they were deployment inputs.

VISUAL GRAMMAR AND ATOMIC FIELDS

Use a restrained scientific illustration style: clean white ground, dark slate text and primary arrows, thin charcoal grid lines, blue student accents, neutral grey teacher accents, teal view-u tags and amber view-v tags. Teacher and student model colors must not substitute for their labels. Mask tiles use a consistent light blue-grey fill with a small slash or mask symbol. A muted violet is available for the loss and EMA annotations, used sparingly and consistently. Do not use green success checks, red failure stamps, rainbow token palettes, glossy shadows, dark canvas panels, background texture, or ornamental brain/database icons.

Keep image grids spatial and patch strips ordered. The selected u:i/u:j or v:k/v:l image cells, student input mask tiles, teacher target distributions and student patch-prediction distributions use matching local identities. Class tokens remain distinct singleton outputs in each LOWER output lane and terminate at C ports; the selected patch distributions in the UPPER lane are reached only by P ports. A repeated C or P port name is a continuation of the same tensor, while different port names represent different token roles or views. The schematic bars are illustrative output representations, not measured activations. Teacher/student strip alignment expresses position correspondence, not equality of feature values. Retain the current grid and token scale so the corrected correspondences remain readable.

ARROW ROUTING, LABEL FIT, AND FINAL REVIEW CONDITIONS

Keep all existing short image-to-model arrows in their row. Every model head has independent class and patch output branches, ending at separate C and P ports. Use matching upper P tags for the local selected-patch distribution fields, matching C entry tags for the lower class comparisons, and the matching G pair for total-loss-to-student-gradient continuation. A port must touch its true source/output edge or have a short explicit local connection to it; no port or gradient arrow floats without provenance. Between the u and v image tags there is no same-signal line. No C port sends an edge into any P strip. The main scientific routes stay short and do not require new long diagonals through the figure.

Use solid slate arrows for forward data, lighter dashed target arrows where appropriate, and the clearly directed EMA arrow for teacher parameter updating. Stop-gradient markers are located on teacher target branches before their CE consumers. Use unarrowed dotted guides only for the patch-example association or true same-position correspondence within a view. Remove the bidirectional same-signal convention between different views. The matching G ports and short local gradient arrow are the only displayed total-loss update route to θ_s. Symbols for masking, stop-gradient and continuation are visually different and do not cover token labels or arrowheads.

Typeset only the short scientific labels already established, plus the precise C/P/G continuation IDs and Learned representation replacement. Retain iBOT, Pretraining, Conceptual illustration, view/mask names, teacher/student f/h/θ notation, shared-head/shared-network labels, patch positions, mask sets, class and patch loss expressions, total L, gradient update and EMA. Do not print source URLs, paper-result numbers, review comments, diagram-version labels, prompt headings, layout coordinates or long cautionary prose. All added labels must fit locally at the existing text scale. The visible outcome has no f_s-only or f_t-only selection.

Inspect the largest empty areas after composing all regions. The upper-left original image and lower-left patch inset must occupy their full intended bounds with meaningful content. Both view rows must show clear image texture, masks, token outputs, and comparison operands rather than empty colored containers. The lower region must be filled by real comparisons and update structure, not a long title or a wide blank separator. Preserve only narrow gaps for individual arrows, paired-input separation, formulas, and grouping. Reclaim an unexplained gap by enlarging the adjacent in-scope image or glyph and moving its labels closer, not by adding decorative icons or redundant examples.

Inspect the four repaired outputs one by one: h_t/h_s class branches terminate at C_ut/C_us/C_vt/C_vs and patch branches terminate at their independent P counterparts; no class output enters a patch strip. Inspect the two view-image branches: both originate from Augment, and no edge joins u to v. Inspect masks: exactly the two tracked cells per view are masked in the student crop and represented at the same selected patch-strip positions, with unmasked same-view teacher targets. Inspect the gradient: total L has a visible local output to G, the matching G enters θ_s, and θ_s still enters θ_t by EMA. Preserve the existing correct cross-view class pairs and dense overall composition. The revised raster must be independently checked; prompt length is not a visual or scientific pass.
