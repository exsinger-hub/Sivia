Create a compact, richly illustrated scientific method overview titled “PDFormer: traffic dependencies across space and time”. This is a new educational rendering of the PDFormer method, with an original symbolic city example. Its message is that geographic, semantic and temporal attention capture complementary dependencies, while historical pattern information enriches geographic keys to account for propagation delay. Draw the method as a tightly connected traffic-and-representation board. Do not present a benchmark, measured forecast, reproduction of a published traffic map or an implemented model run.

LAYOUT AND FIRST READING

Use a landscape canvas with ratio 1.7:1 and a shallow title line. Think of a two-column scientific plate rather than a slide. The left 28% of the body is a vertically connected input scene: road map in its upper half and a compact historical tensor plus embedding construction in its lower half. The right 69% contains a broad attention scene in its upper 60% and a delay-aware mechanism in its lower 36%. A narrow two-percent gutter between the columns carries only the two necessary inputs into the upper right. Let every scene fill its allotted space with identifiable content. The bottom row of the left input scene aligns with the bottom of the lower-right mechanism.

Inside the upper-right attention scene, place three adjacent parallel lanes with unequal widths: geographic 38%, semantic 32%, temporal 30%. Each lane contains a concrete relation view at the top, then a compact attention operation underneath, then its output feature strip. Under these three strips, place one continuous concatenate-and-project band. Immediately below this band, place the small “Feed-forward” block and an output head ending in three large schematic future-state planes. The planes are a sampled symbolic forecast horizon, not a claim that the actual horizon is three. A short label “Future traffic features, T′” establishes the horizon without a numeric value.

Do not isolate the output in a mostly empty far-right column. Keep the output planes horizontally aligned with the fusion band. Use a small outer brace labelled “Repeated ST layers” around attention plus feed-forward; the repetition is a structural annotation. Keep the lower delay-aware mechanism as an expanded explanation of the geographic key update, with a matching D tag at the geographic key interface and the lower key-update output. Do not draw a cross-panel association line. The actual historical-pattern input into the key update is shown within the expanded mechanism and named at the main geographic key interface.

INPUT CITY AND CONSISTENT IDENTITIES

Draw an original stylized overhead road map with six sensor nodes A, B, C, D, E and F. These six letters are illustrative identity markers, not dataset station IDs or the paper's number of nodes. The city is a close crop containing two curving arterial roads and a few cross streets, with a river or park only if it helps separate locations. Avoid decorative skylines, unused map margins and tiny landmark icons. The map fills its frame with meaningful road structure. Put A and B on neighboring segments of one arterial, C farther along that same road, D near a bridge approach, and E and F on a distant parallel arterial. Sensor markers are clear circles with their letter centered.

Use amber to highlight a local geographic neighborhood around A and B. Use violet halos on A and E to indicate a possible semantic relationship between distant locations. The violet relation is an abstract example of traffic-pattern similarity; it is not a second road. Draw the semantic connection as a short dashed association over the map and label it “Similar patterns”. Draw actual roads as neutral gray continuous geometry so the two relationships cannot be confused. Keep the large amber neighborhood glyph in the geographic lane and the violet pair in the semantic lane registered to the same map geometry.

Below the map, show a compact sequence of four small map snapshots with the same six sensors in identical positions. Their small tile values are indicated by abstract shades, not numbers. Label the strip “Historical observations” and put one time arrow below it pointing left to right toward t. A tiny ellipsis before the first displayed snapshot shows that this is only a sample of the full history T. The symbolic shades differ across time; they are not learned attention or measured flows. A small caption “Schematic road network” under the main map and the final conceptual footer make the role clear without occupying a separate status panel.

Beside or below the history strip, construct the historical traffic tensor as four aligned feature planes, each with six small sensor rows. The plane faces all have the same dimensions because the property changing between them is time. Place the label “History X” above and “time × nodes × channels” beside it. Keep a single visible A row in amber across the planes so the reader can trace a sensor through time. The rest of the sensor rows use neutral blue-gray. These row markers establish identity only; they do not mean the feature values remain fixed.

EMBEDDINGS WITHOUT EMPTY MODULE BOXES

The input tensor enters a flat operation titled “Data embedding”. Give the operation enough width for a short local sum depiction beneath its heading. Draw one small traffic-feature slab, one road-structure slab, two periodic-time chips and one position strip as the five operands feeding a small plus junction. Label them “Data”, “Spatial”, “Week”, “Day” and “Position”. These are roles of the embeddings; do not show numeric times, invented eigenvalues or a full calendar. A road-structure branch comes from the main graph into the Spatial slab. The time sequence supplies Week, Day and Position through an explicitly shared time-context association.

The five embedding operands merge into one Embedded features stack H. Print a compact statement H supplies three parallel attention head families. Each attention lane uses H as its named input, so there is no long fan-out bus and no isolated Split heads box. Place a small H chip beside each lane's attention operation. Do not draw any arrow from H or the embedding scene into the lower delay-aware workbench; the latter receives recent traffic history, not the attention-head output.

GEOGRAPHIC LANE

At the top of the geographic lane, repeat the same schematic road map as a tight local crop around A, B and C. A and B lie close on connected road segments. Highlight the permitted neighborhood in amber with a thin halo and short inward arrows toward A. Nearby physical placement and local graph connectivity are the explanatory relation. Put a tiny binary-style neighborhood matrix beside the map, with only the relevant A/B/C row and column identity markers. Amber cells indicate permitted connections and pale neutral cells indicate masked positions. The matrix is symbolic, not the model's learned weights; label it “Geographic mask”.

Under the map and mask, place one tight operator titled Geographic attention. Inside it print the concise interface Inputs: H; Q, K-prime, V; geographic mask. Beside it show three small Q, K-prime and V tiles as labelled operands without individual connecting arrows. A small D tag next to K-prime means key enrichment detailed below. The explicitly named interface makes the operands and mask consumer unambiguous. Do not draw an arrow from the mask to V or any Q/K/V tile. One outgoing arrow from the Geographic attention operator leads to the amber output strip. The strip then enters the fusion band.

SEMANTIC LANE

At the top of the semantic lane, show the same map geometry at a wider crop so A and E are clearly far apart. Their violet markers are joined by a dashed relationship line that is visibly distinct from the gray roads. Beneath the two markers place two small aligned pattern strips that have matching shape motifs but no numeric amplitudes or axes. This makes the meaning of “similar patterns” concrete without synthesizing a measurement. The small strip label is “Pattern similarity”; the road-distance gap remains visible but compact.

Place a small violet “Semantic mask” matrix next to this relation view. The highlighted association connects A and E despite their separation in the road graph. Keep this matrix separate from the geographic matrix; the two masks represent different neighborhood definitions. Under it, draw one Semantic attention operation with the printed interface Inputs: H; Q, K, V; semantic mask. Show the Q/K/V tiles as named operands without separate arrows. One output arrow leads to the violet strip. There is no K′ marker in this lane and no edge from the delay-aware inset into the semantic key. That distinction is essential: the historical delay enrichment shown below belongs to geographic attention.

The semantic output is a violet strip of the same optical size as the geographic output, feeding its own port in the fusion band. The lane header is “Semantic attention”. Do not rename it “Global traffic” or imply that every pair of sensors always attends equally. The example pair only demonstrates how semantic proximity can differ from physical proximity. Use a compact bracket beside the relation view to indicate the pair A–E, with a short text label instead of an unbounded looping arrow.

TEMPORAL LANE

At the top of the temporal lane, display one sensor identity A with four ordered observation tiles at t−3, t−2, t−1 and t. These are schematic displayed positions, not the paper's configured history length. Use a short leading ellipsis if needed so the strip clearly samples a larger sequence. Draw curved local links from earlier tiles toward the current tile to suggest temporal attention over the same sensor's history. The links stay within the compact strip and do not connect to sensors B or E.

Below the sequence, draw a small square relationship grid whose axes are both labeled “Time”. This contrasts with the node–node role of the two spatial lanes. The grid's marks are abstract allowed relation symbols, not learned weights. Then draw one Temporal attention operation with the printed interface Inputs: H; Q, K, V; time axis. Show Q/K/V as named tiles without separate arrows. One output arrow leads to the teal strip. The lane header is “Temporal attention”. Do not attach either spatial mask to this operation. The grid and sequence should occupy most of the top half of the lane, avoiding a blank card with only the three letters Q K V.

FUSION AND PREDICTION

The amber, violet and teal output strips each reach a distinct slot on the band labeled “Concatenate heads → project”. Depict concatenation as three short colored strips meeting side by side into one wider strip; a small projection block then makes one blue-gray output plane. Do not use a plus symbol at the concatenation. A short “Feed-forward” operator follows, with a clear one-direction arrow into “Output head”. Keep these routine operators compact and give the schematic future feature planes enough size to be read as the method's result representation.

Draw two small offset outlines behind the attention/feed-forward enclosure and a brace Repeated ST layers. This is a repetition annotation, not an exact layer count. The Output head consumes the layer outputs; print the short label Layer outputs at its input. Layer-skip routing is intentionally summarized at this overview depth. Do not draw a dangling skip bus or add an S connector. The main fusion -> Feed-forward -> Output head path remains continuous and clear.

The final feature planes have the same sensor-row orientation as History X but a label “Future, T′”. Show one small arrow pointing along their time order. They are abstract output carriers only, without numeric traffic values or a colorbar. A small stylized map field may sit directly beside them to indicate the road-network indexing, but it must retain the original graph and cannot be presented as a measured prediction heatmap. If this extra field makes the output confusing, use only the aligned feature planes and their row identity A.

DELAY-AWARE EXPANSION

Use the lower-right region for a broad, tightly arranged inset titled “Delay-aware key enrichment”. Its left edge contains a short local history strip for node n, shown as a sloped abstract profile in a small framed carrier. Label it “Recent history”. Below it, place three compact pattern glyphs in a row under the label “Pattern bank P”. They depict a descending shape, a plateau and a rising shape with consistent stroke weight. These are example shape motifs, not actual cluster centroids, and three is not the configured number of patterns. A trailing ellipsis indicates a bank rather than an exhaustive set.

Each pattern glyph feeds a small memory tile. The recent-history strip feeds a query-like history embedding tile. Bring the history embedding and the pattern memory tiles to a small “Match” operator, then a short “Softmax” operator produces a row of weight chips. Keep the weight chips symbolic and unequal in emphasis but do not print values. One aligned set of projected pattern tiles supplies the other operands of a “Weighted sum” junction. This makes weighting an actual operation with two operands instead of a label floating above a bank of unrelated curves.

The Weighted sum output is a compact amber vector labeled “R”. On its right, place a separate original key tile K and a plus junction. R and K enter two distinct ports of the plus, and its result is K′. Print the concise equation “K′ = K + R” directly below these three objects. The equation is explanatory shorthand for the geographic key update; do not replace plus with concatenation or feed R directly to the temporal lane. Place a small D tag beside K-prime. D denotes this key enrichment, matching the main geographic interface; no cross-panel line is drawn.

Under the Pattern bank label, add a small, tightly fitted preparation cue: a few cropped time-window strips pass through “k-Shape” into the bank. This cue explains where representative patterns come from. Use a thin group divider so bank construction from historical windows is distinguished from the per-node lookup. Do not draw k-Shape as a trainable online layer inside every attention call. The memory tiles and projected-pattern tiles can share a common pattern identity marker, but they are different projections; their internal values need not match.

USEFUL MICRO-DETAILS

In the city scene, make the six sensor markers crisp and evenly sized; the main interest is their relationships, not photorealistic architecture. In the historical tensor, give the A row a thin amber edge across all sampled time planes. In the semantic pattern pair, use matching turning points but different neutral background shading to avoid implying equal observed values. In the temporal strip, keep every tile associated with A, and use index labels only along the time axis. In the geographic mask, row and column identities must correspond to the same displayed nodes.

Give the main geographic lane enough width to hold both a local map and its mask without shrinking either to an icon. Give the semantic lane enough room for distant-node relationship and pattern motifs as one compact scene. Give the temporal lane enough room for its time strip and time–time grid. Each lane has a distinct concrete carrier before its Attention box, so the figure's richness comes from meaningful views rather than repeated architecture blocks. The three output strips should align precisely at the fusion row.

Use blue-gray for common representations, amber for geographic/delay content, violet for semantic relations and teal for time. Keep roads neutral gray, sensor identity letters dark and the A highlight consistent. Print only the title, the listed short lane/operation labels, A–F and n identity markers, the specified symbolic time labels, Q/K/K′/V/R/P and K′ = K + R, together with the tiny conceptual footer. Do not add numerical forecasts, metrics, citations, author names, a claim of state-of-the-art performance or paragraphs from the source paper. The long production description determines the picture's precision, not its printed text volume.


RENDERING CONTRACT FOR THE SPECIFIED SCIENTIFIC SCENE

Render one complete image from the fully instantiated scientific specification above. The specification is a production document: its paragraphs explain how to place and connect visible objects. Print only the short labels, mathematical expressions and example text explicitly identified as visible. The final artwork is an explanatory method illustration with a small readable footer saying “Conceptual illustration”. This footer should occupy a thin baseline immediately below the last content row. It must not require a separate panel or leave a large empty strip. Do not print the name of this rendering contract or any authoring directions.

DENSITY AND USEFUL CONTENT

The composition must have low uninformative empty area. Use narrow exterior margins of approximately two percent of the canvas width and height, and compact internal gutters. A gutter is a short gap separating two objects or carrying a connection; it is not an empty column between independent panels. Let the scientific scenes reach close to the four sides of their allotted regions. Where a region contains fewer objects, enlarge the actual representation and give its internal structure room to be understood. Resize an entire meaningful scene coherently, including its labels and ports, instead of enlarging a heading or adding a blank tinted container.

Maintain continuous scientific content across the body of the composition at both coarse and fine viewing scales. No large region should consist only of a flat background, a border, one arrow, or a single oversized word. Meaningful visual mass comes from the source-defined inputs, recognizable scene elements, spatial correspondences, ordered representations, operator operands and genuine outputs. Color alone does not create information. A pale colored area between disconnected boxes is still empty. If the proposed arrangement leaves such an area, pull the connected objects together and use the recovered room to enlarge an existing input field or mechanism inset within its scientific scope.

Design the body so that every major horizontal and vertical band intersects useful representations. Offset adjacent scenes only when doing so creates an intelligible hierarchy or permits a short route. Avoid a row of small boxes aligned along the top of a large empty card. Avoid a tall central feature stack surrounded by white triangles. Use the exact proportions given in the case specification, then fit local geometry tightly within them. A pair of corresponding panels should have aligned view windows; a panel with a different function may have a different width. Equal panel sizes are not a default.

Retain functional breathing room at arrowheads, formula baselines and sample boundaries. Dense should mean that the reader sees more scientific relationships, not that objects touch arbitrarily. Leave at least one small text-height of clearance around a central merge symbol and enough gap for an arrowhead to be seen as an endpoint. Do not reduce important labels to hairline text to squeeze in prose. If there is a fit conflict, shorten a permitted local label through line breaks or reduce the space between its graphic operands. Do not silently omit a required input or collapse distinct operations.

SCIENTIFIC GLYPHS AND IMAGE FIELDS

Construct each image field as a deliberate rectangular viewport containing the specified scene. Its crop must include the objects needed for the illustrated operation; remove gratuitous sky, blank walls or empty floor that would make the frame seem sparse. A viewport is a scientific carrier, so its area may be filled by scene texture only when that texture belongs to the declared scene. Keep matched fields registered to the same conceptual sample: maintain object count, orientation, silhouette, relative position and color whenever the operation preserves them. A detail crop has a visible association to its source field and cannot become a newly invented sample.

Make symbolic feature fields visibly different from natural image fields. Feature planes may use small colored cells, diagonal hatching or a few grouped marks to suggest channels and positions, but do not present them as computed activation maps. Use clean abstract patterns with no colorbar, decimal values or heatmap performance legend. A token strip has a clear reading order and short identity markers. A spatial grid has a clear two-dimensional arrangement. A graph has nodes and edges with a declared adjacency meaning. These carriers should remain identifiable in grayscale by shape, enclosure and annotation, not just color.

Use only the number of illustrative cells or nodes specified in the scene. When a display samples a larger set, an ellipsis at the end of the local strip indicates omitted repeated items, and the set label remains symbolic. Do not write a precise channel count or number of layers unless the case explicitly provides it as a verified quantity. If a stack represents scales, vary its face size while keeping the same image identity. If it represents channels, hold the face size constant and use a small depth offset. If it represents time, use a sequence of matching panels and indicate the direction once.

Treat masks as meaningful spatial supports. The mask viewport must have the same aspect ratio and object registration as its corresponding image viewport. White and black encode the declared binary convention; do not invert that convention in a neighboring panel. A missing-data mask is not automatically a target segmentation mask. A difference mask represents the specified comparison of supports, not uncertainty, error probability or confidence. Keep masks visually flat and crisp, with no decorative lighting or translucent photographic texture inside their binary support.

LOCAL CONNECTION GEOMETRY

Every scientific edge has one identifiable source and one identifiable destination. Start a main arrow at a representation boundary or an operator output port, and terminate at the input side of the operation that consumes it. Do not terminate a data arrow on a panel title. At a split, one short trunk reaches a visible branch dot, then separate branches depart toward their actual targets. At a merge, separate operands remain visually independent until they reach the explicit merge symbol. Mere proximity or placement inside the same enclosure does not communicate a dependency.

Use short orthogonal routes for long-distance data connections and a gentle bend only where it prevents a crossing. Reserve slim routing lanes along the edge of a region, but keep them adjacent to the content they serve. A route should not travel around the entire figure unless the source-defined feedback truly returns to an early stage. Even a return edge should use one compact outer lane with a clear arrowhead at its consumer. Distinguish a data return from a dotted explanatory association to an expanded inset. The inset association has no arrowhead suggesting that the inset computes an extra downstream result.

If two independent lines must cross, use a small bridge or a clean uninterrupted crossing without a junction dot, and keep the crossing away from labels. Junction dots indicate actual joins or branches only. Avoid running connectors directly beneath text, through image viewports or behind a large group header. A line hidden by an operator box cannot be assumed to connect to it. Give each multiply, sum or concatenate operation a distinct visible glyph or a short operator label. Addition, concatenation, selection and conditioning must not share an unlabeled circular node.

Align parallel branches at their entry and exit ports so the eye can compare their operation order. Do not force a parallel architecture into a single serpentine chain. When two paths share a representation, draw a branch from that representation; when they use independent representations of the same type, draw separate objects. A repeated module name does not mean weight sharing, and a repeated sample icon does not mean a new sample. Use the identities and scope markers provided in the case specification to make these distinctions visible.

TYPOGRAPHY AND HIERARCHY

Use one clear sans-serif family with a slightly heavier weight for panel headings and operation names. Mathematical symbols may use a compatible serif math face, but their baseline and optical weight should sit comfortably with the neighboring technical label. Set the main title at about one and a half times the body-label size, not several times larger. Panel headings are only modestly larger than body labels. Keep the title height shallow so that the body begins near the top edge. The scientific scene is the visual focus.

At the intended full-image display size, the smallest necessary technical labels must remain comfortably readable. Use a limited number of short lines inside a document or description card; do not simulate paragraphs with tiny illegible text. Place a text label immediately next to the object it names. Use a maximum of two lines for ordinary operation names, with line breaks at semantic boundaries. Avoid stretching letter spacing to fill empty width. Important index letters, a prime on a forecast horizon, and the distinction between a feature value and a positional label must be legible.

Place tensor or set notation directly beside the representation. Put units or axis roles beside the axis they describe. A legend should be small, close to the relevant scene and used only when the same visual grammar recurs enough to justify it. Do not repeat a full legend in every panel. Do not give illustrative token shades a numeric scale. A concise note such as “symbolic”, “example”, “training” or “inference” can establish scope where the case requests it, but internal evaluation status and production bookkeeping stay outside the artwork.

COLOR, MATERIAL AND STROKE

Follow the case's role-based palette. Use white as the main canvas and restrained light fills behind tightly fitted groups. Use dark charcoal for body text and main outline work. Give each scientific family one accent and a darker version for its border or port. An input image may naturally contain several colors without redefining the semantic palette of model components. Colors used to track an object through a transformation must stay stable across all views of that object.

Use consistent medium-weight strokes for image frames and operator boundaries, slightly heavier arrows for the main pathway, and thinner dotted strokes for explanatory associations. Avoid thick black outer borders that visually divide the canvas into disconnected boxes. Rounded corners may be subtle for grouping, but flat operator blocks and planar fields should dominate. Small offset planes may create modest depth where they explain a representation stack. Do not add glossy reflections, floating shadows, lens flares, decorative neural-network backgrounds or ornamental circuitry.

For visual depth in scene thumbnails, use clear foreground/background separation and enough local contrast to show the designated object. Keep lighting consistent between matched images. A changed illumination or camera viewpoint should not masquerade as the output of an algorithm that preserves geometry. Where the specification calls for a physical object silhouette, draw that silhouette precisely rather than substituting an unrelated stock icon. Where it calls for an abstract state, do not replace the state with a face, a robot or a generic brain symbol.

COMPLETENESS AND FINAL FIT

Ensure the reader can follow the entire specified input-to-output path without consulting a companion figure. Routine operators may be compact, but the distinctive transformation needs visible operands and a changed representation. Where the case includes a supporting mechanism inset, keep that inset visually subordinate to the main path while making its correspondence unambiguous. The inset should explain an operation already present in the main pathway; it must not introduce a new unverified component just to increase complexity.

Inspect the intended drawing as three nested readings. The thumbnail should show a coherent scientific scene with low unused area. At ordinary reading distance, module roles, branches, scope changes and outputs should be apparent. At closer inspection, the exact object identities, mask correspondence, attention axes or source-defined equations should be recoverable. These readings must agree; an attractive silhouette does not excuse an incorrect arrow or an output that changes object identity.

Finish with a clean edge-to-edge arrangement of the required content. Remove oversized spaces beneath terminal outputs, around tiny formulas and above small panel headings by refitting the real content locally. Preserve the necessary connector clearances while doing so. Keep all important labels inside the canvas, all image fields fully visible and all required arrows unobstructed. Render the complete illustration at high resolution with an opaque white background. The final deliverable is one scientific conceptual figure, not a contact sheet, a presentation slide with bullet paragraphs, or a screenshot of these production instructions.
