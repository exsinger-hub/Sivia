PRECISION EDIT: Use the supplied Self-RAG v2 image. Change only the small lower training annotation: remove the bracket under d and ISREL and write the short text d: no LM loss directly below the blue d token alone. ISREL remains supervised. Preserve every inference row, N/T port, selected answer, layout, size, color, and other visible element. The complete final specification follows.

Create one compact, publication-style scientific overview titled “Self-RAG: Retrieve, Reflect, Select”. It explains the inference and training distinction in Self-RAG: Learning to Retrieve, Generate, and Critique through Self-Reflection. Use a white 3:2 landscape canvas, approximately 1800 by 1200 pixels or a larger equivalent, with a meaningful-content hull extending nearly to the four edges. The intended printed width is 180 mm. Print the small phrase “Conceptual illustration” once at the upper right. The example documents and answer below are deliberately fictional teaching content; they are not reproduced retrieval results, empirical evaluations, or evidence that this model has been run.

The visual claim is that a single trained language model decides whether a segment needs retrieval, generates passage-conditioned candidate segments, emits distinct reflection tokens, and uses those reflections in selecting a segment. At first glance the reader should see three aligned passage-to-candidate lanes feeding one selected segment. At closer reading, the important distinctions are passage relevance versus candidate support, optional retrieval versus direct generation, and an offline training critic versus the deployed generator. Give these real relations the visual space. Avoid an oversized title, a row of empty generic model cards, a floating robot mascot, and a broad vacant bottom band.

Use normalized canvas coordinates from 0 to 1000 only as drawing guidance, not visible labels. Keep the usable area within x=18..982 and y=15..985. The title occupies y=18..64, with no decorative banner behind it. A compact input-and-retrieval rail occupies y=82..204. The principal three-lane correspondence scene occupies x=20..802, y=226..682. A narrow selection-and-output region occupies x=824..980, y=226..682. A direct-generation branch occupies y=704..790 across most of the width. A short, visually distinct training scene occupies y=818..982. The inter-region spaces are narrow routing gutters, not blank exhibition areas. If text would be too small, shorten only labels specified as optional, expand the corresponding object into nearby whitespace, and maintain this dense content silhouette.

The title must be modest relative to the method: approximately 27–30 pt at the intended width; section labels about 12–14 pt; the key excerpt and relation labels about 9–10 pt; secondary symbols at least about 8 pt. Use a clean scientific sans serif with separate, consistent math glyphs for x, d_j, y_<t, and y_t. No handwritten font or brush underline is needed. Most of the canvas should consist of readable short excerpts, aligned candidate objects, token identities, actual connections, and a compact training-data transformation. Empty pale color does not count as scientific content.

TOP INPUT AND OPTIONAL RETRIEVAL

The supplied image is the v2 draft of this same figure. Preserve its tight, readable matrix, all three fictional passage/candidate sentences, all reflection values, selected d_1 answer, typography, and compact training strip. Repair only the top retrieval/conditioning connections, the No branch origin, the append continuation, and the spurious learned-generator arrow. These corrections replace the previous long-wire layout with a few local arrows and explicitly matching continuation markers. Keep content density at least as high as the reference; the removed lines do not create a need for new empty bands or enlarged container fills.

At x=20..330,y=88..178 keep the flat input sheet titled “Input x”, with the query “Where is parcel Q stored?” and the small fictional Q parcel. Its bottom bar reads “Previous segments y_<t”. The right border of Input x connects by one dark solid arrow to “M: Retrieve?” at x=367..505,y=102..172. Keep its distinct Yes and No ports on the right boundary. The Yes port connects by one short blue arrow to “Retriever R”, x=555..669,y=97..163. The No port has only a very short local dark arrow ending at a circular continuation badge “N” beside the port. There is no long line from No back to the input sheet, and no line from this port crossing the matrix.

At x=705..973,y=85..178 replace the top-right Passage collection title with “Retrieved set D”. Keep three compact document slips labeled d_1, d_2, d_3. Draw one blue arrow from R's right boundary to the left boundary of this retrieved-set box. Label it “retrieve”. Remove the previous reverse returned-passages arrow and its label, remove the old query edge label, and remove any bottom arrow pointing upward into R. This scope depicts R producing the retrieved set; the external collection's internal search is compacted inside the retriever. Do not show a collection query/return cycle in this revised view.

The three main row passage ids are the same d_1, d_2, d_3 members of D. Add the compact matrix header “For each d_j in D” above the Retrieved passage column. This explicit set-membership correspondence links the set to the aligned rows without a long branching wire. Do not draw any direct line from the bottom d_3 passage into the No-retrieval branch. The three documents are parallel alternatives, never stages in a serial chain.

Remove the entire long blue x,y_<t conditioning rail above the matrix, including arrows incorrectly entering ISREL or ISSUP. Replace it with one small note above the Generate with M column: “Each M: x, y_<t, d_j”. The row's document-to-relevance-to-M arrows remain. The context note is a local declaration of inputs to each call, not an edge into any reflection token. Keep the thin same-M association bracket beside the repeated M badges without arrowheads. There is no shared blue bus spanning the top row.

Attach a small circular continuation badge “T” at the right end of the Previous segments bar, with one short inward arrow from T into that bar. Attach a matching “T” badge immediately beside the Selected segment output, with one short outward arrow from that output to T and the concise label “append; next segment”. These two identically lettered ports express the only cross-region continuation. Remove the old long outer-right feedback loop completely, including its stray arrow stopping near the retriever. A T marker is a connector continuation, not a new model, training target, or retrieved passage. Place N and T badges in small available gutters; they must not overlap existing labels.

The top rail now contains a simple continuous Input x → M: Retrieve? → Yes → R → Retrieved set D path. The No outlet ends at N. The separate T pair connects selected output back to prior generated context. These are distinct roles with distinct markers, and neither N nor T is attached to a document d_j. Keep local arrows at visible boundaries and remove all remnants of the deleted long routes.

THREE PASSAGE-CANDIDATE LANES

Create three closely spaced horizontal lanes, with rows centered at y=307, y=455, and y=603. Their outer row bounds are approximately y=240..373, 388..521, and 536..669. Use thin light horizontal separators or a subtle row identity tint behind only small row tabs. Do not fill three large rounded cards with color. Within each row, align the passage sheet at x=42..251, relevance token at x=278..365, generated segment at x=405..611, and support/utility token group at x=638..800. The space between objects is for arrowheads and short labels. Keep all three row baselines, document ids, candidate positions, and reflection fields registered vertically.

Place the column headers just above the first lane, not in a separate tall band: “Retrieved passage”, “ISREL”, “Generate with M”, and “ISSUP / ISUSE”. Give the header about one text line of height. The three model operations should be recognized as repeated calls to the same trained generator M, not three independently trained experts. Use the same small M badge on each generated-segment box and a thin shared-parameter bracket beside the column, labeled only “same M”. This bracket is an explanatory association without an arrowhead; it must not be mistaken for a fourth computation branch.

For lane 1, draw a folded-corner paper sheet labeled “d_1”. It contains the exact excerpt “Parcel Q is stored” on the first line and “in locker B.” on the second. Beside the excerpt draw a compact two-door locker sketch with only the relevant door carrying a B label and a small Q parcel inside its outline. This sketch illustrates the sentence for the reader; do not draw a separate modality arrow from it to M. The sheet is mainly readable text, with the locker glyph occupying no more than its rightmost quarter. Use a narrow dark-blue d_1 tab that repeats in this lane’s generated segment and selected output.

The lane-1 passage sheet’s right-center border connects to the left border of an ISREL token tile at x=278..365. This tile prints “Relevant” in a short blue token-shaped capsule. Its top header remains the shared column label ISREL, not another model. Continue from the token tile’s right-center boundary to the small M badge at the left edge of the candidate sheet. This ordering depicts relevance reflection associated with the passage before the passage-conditioned candidate is shown. No green check or truth shield belongs on the relevance tile.

The lane-1 candidate sheet at x=405..611 prints “Parcel Q is stored” and “in locker B.” on two readable lines. Put the short label “y_t,1” at its upper left and a d_1 citation tab at its lower right. The body text matches the passage in this teaching example. A short blue arrow enters the M badge from the relevance tile. The shared-input declaration “Each M: x, y_<t, d_j” above the column supplies the conditioning scope. Do not add a long top conditioning wire. The relevance token alone is not the passage, and previous generated segments are not retrieved evidence.

From the lane-1 candidate sheet’s right boundary draw a dark short arrow into the reflection-token group at x=638..800. Its upper token shows “Fully supported” beneath the small label “ISSUP”. Its lower small token shows “ISUSE” followed by the symbolic value “u_1”. Keep u_1 as a model-produced utility token placeholder, with no numerical score, gauge, checkmark, measured bar, or success percentage. In the far-right edge of the group, place a tiny d_1 tag beside a candidate-text miniature, showing that the outgoing bundle includes the candidate and its reflection tokens together.

For lane 2, draw another paper sheet with an identical shape, labeled “d_2”. It prints “The loading dock” and “closes at dusk.” Include a small line drawing of a dock door and a low sun to illustrate that this document is about a different fact. Do not make the irrelevant passage illegible or crumpled: it may contain a coherent supported statement while failing to answer this query. Its ISREL tile prints “Irrelevant” in an amber outlined capsule. The relevance relation is about x and d_2, not a judgment that all of d_2 is false.

Lane 2’s candidate sheet, labeled “y_t,2”, prints “The loading dock” and “closes at dusk.” It carries a d_2 tab. The ISSUP token prints “Fully supported”, because the candidate statement is supported by its own passage even though it does not answer the query. Its ISUSE token uses “u_2” without a numerical value. This combination is the central teaching distinction: irrelevant-to-query and supported-by-passage are different predicates. Preserve both words exactly. Do not automatically change the support token to “No support” merely because the relevance tile is amber. No red cross should obscure the candidate sentence.

Keep the same input routing in lane 2: d_2 sheet → ISREL tile → M candidate operation → ISSUP/ISUSE group. The same local input declaration above the M column applies to this repeated call. A narrow row tab carrying the d_2 identity remains visible at every source-linked point. The icon and sun are part of an original fictional reading aid; they are not copied paper photographs or evidence from an executed retriever. Use the same sentence widths and type scale as lane 1 so that the semantic distinction, rather than size, carries the comparison.

For lane 3, draw a sheet labeled “d_3”. Its two-line excerpt is “Parcel Q was moved” and “into a locker.” A tiny locker sketch here has no letter on its doors. This deliberate absence carries useful information: the passage does not identify locker B. The ISREL tile prints “Relevant”. The candidate sheet labeled “y_t,3” nevertheless prints “Parcel Q is stored” and “in locker B.” and carries a d_3 citation tab. In this conceptual candidate, the named locker exceeds the passage’s information.

The lane-3 ISSUP token prints “Partially supported”. Add a short amber underline directly under “locker B” in that candidate sentence, stopping short of the citation tab. The underline is a local support mismatch cue, not a global model-failure claim. Its utility token uses “u_3”. Do not invent a missing document, repair step, fact checker, or external search branch to make the candidate correct. The overview should let the reader compare this row with lane 1: the wording can match while the supporting passage differs.

For each of the three ISSUP judgments, draw a very light dotted explanatory association from the associated passage sheet’s lower-right corner to the ISSUP tile’s lower-left corner. Route it along that row’s bottom edge beneath the candidate sheet and keep it visually subordinate to the main generation arrows. This association explicitly indicates that support compares candidate and passage. It has no arrowhead because it is a scope guide for the comparison, not a separately executed post-processing pipeline. Give it one shared label under the matrix: “support is relative to its passage”. Do not connect lane 1’s document to lane 3’s support tile, or let the three bottom guides merge.

The visible u_1, u_2, u_3 tags represent members of the model’s utility-token vocabulary, not free continuous confidence estimates. The compact vocabulary key in the training scene defines ISUSE as values 1–5 without assigning measured values to these particular fictional candidates. No confidence plot or ranked probability chart appears. This keeps the figure faithful to reflection-guided selection without presenting uncomputed probabilities as data.

SELECTION, SEGMENT OUTPUT, AND CONTINUATION

At x=831..977,y=246..447 draw a slim selection enclosure titled “Select y_t”. Give it three horizontally aligned input ports on its left edge, one associated with each candidate lane. Since the lane centers are lower than the enclosure, use a slim vertical lane bundle beside x=814 that approaches three staggered entry heights; the three lines remain separated by a small, constant gap. Each incoming line begins at the right edge of its own candidate-plus-reflection group, never at the document id alone. If there is insufficient room to collect all ports without crossings, extend the selection enclosure downward into y=497 rather than hiding edges behind labels.

Inside the selector, show three compact stacked candidate strips marked d_1, d_2, d_3. Each strip contains a short line and the two relevant reflection-token miniatures, so the selector reads as combining text likelihood and model reflections in decoding, not as sorting documents by a retrieval score alone. Print “reflection-guided” above the miniature stack and “decoding” below it, both small. Outline the d_1 strip with a teal border. Keep the other two strips neutral; they remain visible candidates, not discarded truth values. Do not put numeric ranks, a star trophy, an accuracy gauge, or a claim of guaranteed correctness beside this choice.

A single teal output arrow from the selector’s lower border enters an output sheet at x=829..979,y=525..664. Label the sheet “Selected segment y_t”. Print “Parcel Q is stored” and “in locker B. [d_1]”. Keep the citation tab attached to the exact sentence. This is the candidate selected in the conceptual scene, not an objectively verified answer. Use a plain output-sheet icon, without a validation shield or a green success check. The selected sentence has the same wording and document identity as candidate y_t,1.

The Selected segment output has one local outward continuation to badge T, matching the T marker at Previous segments y_<t. Label the output-side marker “append; next segment”. Do not draw a long feedback wire, a free-floating arrow, or an edge from the selected segment into R. The selected text is appended to prior generation for another segment decision; it is not indexed as an external passage. The short T connections replace the old outer loop entirely.

The diagram focuses on the Yes/No paths of one segment step. It does not depict every decoding option or prescribe a fixed beam size, retrieval count, reflection weight, or threshold. The visible Continue behavior is the clear return to preceding generated segments; do not add a speculative repair gate, evidence-verification agent, hidden-state monitor, or multi-agent voting system. This is Self-RAG’s trained generator with its reflection tokens, not the verifier architecture of a different method.

DIRECT GENERATION WITHOUT RETRIEVAL

At x=75..290,y=711..782 keep the lower reused-context strip labeled “Same x, y_<t”. Put a circular N marker directly at its left, with one short dark arrow from N into the strip. It matches the N badge beside the upper model's No outlet. Print “No retrieval” beside this lower marker. Remove the existing lower-branch long line rising into the d_3 sheet. No document border, support association or training object is the source of the No path. This is the alternative branch of the same segment decision, and matching N ports show its origin without any crossing.

At x=345..535,y=713..781 place “M: direct generation” with a short text-token strip. A dark arrow begins at the reused-context strip's right boundary and ends at this operation's left boundary. A small M badge matches the three passage-conditioned calls. The main purpose is to show the absence of mandatory retrieval, not to claim that the illustrated factual question must take the No branch. This is an alternative control path, and the concrete retrieval-based answer above remains the one expanded teaching example.

From the direct M operation’s right boundary, draw a dark arrow into an output sheet at x=575..795,y=711..782. Label it “Direct segment y_t”. Show two short text-token strips rather than printing an invented factual answer. Beneath the strips add a small “ISUSE” token with symbolic value “u”. The direct generation has a utility reflection, while relevance and support tokens tied to retrieved passages do not appear on this branch. A short relation label at x=823..975 reads “utility-only reflection”; below it, print “no passage”. Keep this explanatory pair compact and show one small empty passage outline only as an absence cue, not as a document consumed by M.

Do not draw the direct-output sheet joining the selected parcel answer: Yes and No are alternative paths at a segment decision, not fragments to concatenate. The lower branch has no edge to d_1, d_2, d_3 and no citation badge. Its input remains the same query and prior generation. If space is tight, narrow this row while keeping the context strip, direct M operation, text-token output and utility reflection distinct. Optional retrieval must remain understandable without a second independent example or a long prose note.

OFFLINE TRAINING SCENE AND CRITIC BOUNDARY

At y=818 draw a thin horizontal separator and the compact label “Training: offline reflection annotation”. This lower scene is about learning how to emit reflection tokens, not another inference branch. Arrange it in one tightly packed left-to-right row. At x=26..235,y=848..960 place a small stack of paired input/output training sheets. Each sheet has two rows of short dark text strokes; a distinct retrieved-passage insert is pale blue. Label the stack “x, y + passages”. There are no benchmark values, sample counts, model sizes, or fabricated training trajectories.

At x=268..415,y=849..945 put a small operation labeled “Critic C”. Its input arrow starts at the training stack’s right border and enters C’s left border. Inside C, show four small token silhouettes with abbreviations “Ret”, “Rel”, “Sup”, and “Use”. These silhouettes make the critic’s annotation role visible. A rightward arrow from C ends at an annotated training sequence at x=454..727,y=845..963. The sequence alternates plain text spans, a blue passage span, and colored reflection tokens in a compact line that wraps once only if needed.

The annotated sequence is titled “Interleave text + reflection tokens”. Print a concise symbolic segment such as “x | Retrieve | d | ISREL | y | ISSUP | ISUSE”, with each named item on a separate small token block along the shared baseline. Make the d block blue and place the tiny note “d: no LM loss” directly beneath the blue d token alone, without any bracket or underline spanning neighboring tokens. This depicts the retrieved text’s exclusion from the generator’s loss, not deletion of the passage from its conditioning context. Keep the arrow into the annotated sequence attached to the sequence’s border rather than pointing at the no-loss annotation.

At x=756..977,y=849..961 keep the generator-training operation “Train M”, with “predict text + tokens” and its plain loss glyph. The annotated sequence connects to its left border by one short arrow. Remove the entire optional dashed “learned generator” association from the existing image, especially the erroneous arrow entering the empty passage icon in the No-retrieval row. Do not replace it with another long arrow. The repeated name M and the explicit Training scope are sufficient to show which generator is learned; C has no runtime connection.

At the bottom of the training row, fit a single compact vocabulary line: “ISREL: relevant / irrelevant   ISSUP: full / partial / none   ISUSE: 1–5”. It defines token categories, not measured qualities of the displayed run. Keep the text no smaller than the secondary label scale and do not add an extra legend panel or decorative footer. The full wording “Fully supported” and “Partially supported” remains in the main lanes where it matters. The integers 1–5 are the source-defined allowed utility-token values, not a score assigned to the artwork or an observed model outcome.

The critic has no arrow into any inference-time M box, retrieved passage, candidate support token, or selected answer. Its arrow exits only into the offline annotated corpus. The deployed generator itself emits the reflection tokens shown in the inference rows. This boundary is a scientific invariant. A diagram that introduces a mandatory external runtime critic would explain a different method even if it looked attractive.

COLOR, GEOMETRY, AND FINAL RENDERING

Use dark charcoal #263340 for technical text and normal generation arrows; blue #347CB3 for retrieved-document identity and retrieval content; muted teal #258B76 for the selected segment outline; amber #C78E2E for the local irrelevant or partial-support distinction. Reflection-token shapes may use very light versions of these hues, but a background wash cannot substitute for printed meaning. The offline training row uses a restrained violet outline #78669E. No red-versus-green global grading scheme is necessary. Preserve meaning in grayscale through token words, line styles, row identity tags, and the selected outline.

Document sheets have nearly square corners, a short folded top-right corner, and consistent line spacing. Candidate sheets have plain rectangular outlines with the small M badge at their entry. Reflection tokens are compact capsules with clipped or modestly rounded ends. The passage collection is a shallow stack, not a database tower larger than the method. A parcel or locker sketch is a small concrete reference to the sentence, never a large mascot. The only mild depth is the offset of overlapping document sheets; operators, arrows, and tokens remain flat.

Use two connector prominence levels. Main passage-to-generation-to-reflection-to-selection arrows are about 1.0–1.2 pt at the intended print size, with small clear arrowheads. Row support associations and local continuation markers are about 0.65–0.8 pt and visibly lighter or dotted. Keep line endpoints fully visible at object boundaries. Give labels small white local clearances only where needed, not opaque large boxes that sever the line. Never run a route behind a printed sentence, through a token label, or between a document’s fold and body as if it entered nowhere.

All matrix arrows must be attached to physical source and destination borders. Support scopes pair each candidate only with its own d_j. The selector must see all three candidate/reflection bundles. The selected output receives one outgoing line from the selector. The T continuation terminates at y_<t, and the N continuation begins at the upper No outlet. The retriever consumes the query context and produces D; it does not receive ground-truth support labels. The offline critic produces annotation tokens for the corpus; it does not judge the selected output during the depicted inference step. Keep these different edges distinct even at reduced size.

The required printed text is the title, Conceptual illustration, Input x, the parcel query, Previous segments y_<t, M: Retrieve?, Yes, No, Retriever R, Retrieved set D, retrieve, For each d_j in D, Each M: x,y_<t,d_j, N, T, the four matrix headers, d_1/d_2/d_3, the exact short passage and candidate sentences, y_t,1/y_t,2/y_t,3, Relevant/Irrelevant, Fully supported/Partially supported, ISSUP/ISUSE, u_1/u_2/u_3, same M, support is relative to its passage, Select y_t, reflection-guided decoding, Selected segment y_t, the selected sentence with [d_1], append; next segment, No retrieval, Same x,y_<t, M: direct generation, Direct segment y_t, utility-only reflection, no passage, Training: offline reflection annotation, x,y + passages, Critic C, Interleave text + reflection tokens, the symbolic sequence, d: no LM loss, Train M, predict text + tokens, and the compact vocabulary line. These are short labels and example text; do not print any of the long production instructions or coordinate specifications.

Render the scientific method itself as the dominant visual mass. The matrix must be large enough that the distinction between relevant-but-partially-supported and irrelevant-but-supported is immediately readable. Keep the top retrieval rail short, the right selection region filled by actual candidate strips and output, and the bottom training strip filled by the corpus transformation rather than long prose. Every sizable blank region needs a genuine local job; if a region can be closed without creating crossings, close it and enlarge the meaningful representations. Do not add result graphs, generic performance claims, empty dashboard cards, decorative formulas, or background textures to imitate density.

The final image should be an original educational scientific overview grounded in the stated method, with exact text and concise source-faithful boundaries. No raw paper screenshot, copied paper layout, real corpus logo, fabricated benchmark statistic, or empirical accuracy claim appears. Return one complete corrected raster figure, not a collage of alternative layouts. The full specification here is the complete revised production prompt; preserve all unaffected visible content of the supplied v2 reference. Its density comes from the aligned evidence-to-segment correspondences and the clearly separated learning mechanism, while its labels remain short enough to read.
