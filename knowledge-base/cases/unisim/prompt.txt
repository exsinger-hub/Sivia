PRECISION EDIT of the supplied opaque-white UniSim v3 figure. Freeze all three main interaction columns, their scenes, arrows, labels, H1/H2 ports, model key, and the five source cards. Rebuild ONLY the existing lower-right Training operation. Give clean video, sampled noise, and noisy video three distinct objects. Clean video and sampled noise independently enter Noising; its noisy-video output enters Train video denoiser. Only predicted noise exits the denoiser and enters MSE. The sampled-noise tile branches to Noising and an emitting N port; its matching N enters MSE as the target. N must originate at sampled noise, never Noising or noisy video. Keep h, T5 text, and controls if available as separate denoiser conditions. Remove this training block's internal 3D network decoration to fit the real operands and short arrows. Preserve its current footprint and density. Full synchronized specification follows.

Draw one original, tightly composed scientific METHOD OVERVIEW titled “UniSim: Actions In, Video Out”. The scientific source is Learning Interactive Real-World Simulators, ICLR 2024, especially Sections 2.1 and 2.2 and Figures 1 and 2. The figure explains a learned observation-prediction model: a temporally extended action and finite recent visual history condition video diffusion to generate the next observation segment; sampled observations can become the history for another interaction. Show this as three populated interaction columns with explicit history correspondence, plus one compact source-and-training row. This is a complete method overview, not a result gallery or a reproduction of the paper's robot video pixels.

Print “Conceptual illustration” once, small, at the upper right. All robot, object, room, and human scenes are newly drawn conceptual visual carriers. The depicted motion sequence is not an actual UniSim execution, measured rollout, reproduced success case, or evidence of real-world transfer. Do not draw success rates, rewards, benchmark bars, confidence scores, robot brand names, dataset sample counts, hardware costs, or an invented performance curve. The source-supported conditional dependence is the scientific content; the concrete tabletop scene makes it visible without claiming empirical results.

Use a white 3:2 landscape canvas, roughly 1800 by 1200 pixels or a larger equivalent. Design for a 180 mm printed width with the principal labels about 9–10 pt, section labels about 12–14 pt, and secondary math about 8 pt or larger. The title is about 27 pt and consumes only a shallow line at the top. Keep the outside margin close to 2% of canvas width. The content must extend across the usable area and nearly to the lower edge. Do not place the figure inside a large pastel board, add an empty headline banner, or reserve a broad caption space inside the artwork.

The following coordinates are normalized construction guidance, never printed content. Use x=18..982 and y=15..985 as the meaningful-content hull. The title and short model definition occupy y=18..70. Three interaction columns occupy x=20..318, x=352..650, and x=684..982, from y=93 to y=752. A narrow divider at y=786 separates the training/source row at y=804..978. The two vertical column gutters are for short continuation badges and scope separation; they should not widen into empty towers. The three columns have aligned history/noise inputs, compact denoisers, and generous output-image fields rather than equal empty cards.

Use direct scientific scene silhouettes on white, with thin frames only around each individual observation or noise field. A field should be filled by its own relevant image content; an empty colored viewport does not satisfy this requirement. Give the generated observation strips the largest area in each column, because the visual consequence of an action is the method's output. The preceding history fields are smaller but large enough to recognize the same cube, bowl, gripper, and camera geometry. Keep the training row concise and populated with source visual types and training operands, not paragraphs.

At first glance the reader should recognize three successive interactions, each having recent images plus a different form of action and yielding new images. At working scale the reader should trace history H1 and H2 from one output into the following column. At close scale the reader should distinguish channelwise concatenation of history and fresh noise from the later autoregressive reuse of generated observations. These are different operations and must use different visual carriers. A single broad “Universal Simulator” bar with arrows entering at random places would lose the important interfaces; use three small repeated calls of the same model instead.

CONSISTENT TABLETOP WORLD AND TEMPORAL IDENTITY

The main conceptual scene is a simple tabletop viewed obliquely from the front. Use a light neutral table with a visible back edge, a small blue cube on the left half, an orange shallow bowl on the right half, and a white two-finger robotic gripper entering from the back-left. Add a small dark square mat at the rear of the table as a registration landmark. The cube has a small white circular mark on its front face. The bowl has a tiny dark notch on its near rim. These persistent details let the reader identify the same objects across history and generated fields. The scene is schematic naturalistic illustration, not a photoreal claim about a real robotic platform.

Use the same table, cube, bowl, gripper geometry, lighting, and neutral background in every main-scene field. The first two interaction columns keep the camera fixed. The third demonstrates a camera-type action, so its generated views may rotate the viewpoint slightly while preserving the world-relative object arrangement. Do not create or delete the cube, exchange its identity with the bowl, change its blue color, add a second gripper, or teleport an object. No drawn physical trajectory needs a numerical distance, angle, force, or duration. Small motion arrows inside a field are explanatory overlays, separate from the model's data arrows.

The initial history shows the blue cube well left of the orange bowl, with the gripper approaching from above-left. The first generated segment shows a modest approach motion: its first frame keeps a small gap between gripper and cube, and its final frame brings the fingers close to the cube's left side. The cube remains in the same place. The second generated segment depicts the language action “push cube right”: first contact is visible in the initial output frame, and the final frame places the cube farther right, closer to but not inside the bowl. The third generated segment depicts “turn viewpoint right”: camera perspective changes while the already moved cube and bowl preserve their relationship on the table.

Keep the image positions from the first two columns registered. The cube's relative location in the end of segment o_1 must exactly match the cube in the next column's history h_1. Similarly, the moved cube at the end of o_2 must match the initial history h_2 of the final column. Reusing these scenes is scientific correspondence, not decorative repetition. In a history field, the rightmost or nearest-to-now frame is explicitly the most recent one; do not treat a future generated image as input to its own generation. Show only enough frames to make the correspondence visible; the drawn frame count is schematic and does not fix the model's temporal window.

THREE ALIGNED INTERACTION COLUMNS

At the top of the first column print “Interaction 1” and below it the small symbols “h_0, a_0 → o_1”. At the top of the second print “Interaction 2” and “h_1, a_1 → o_2”. At the top of the third print “Interaction 3” and “h_2, a_2 → o_3”. These symbols describe interaction boundaries, not diffusion timesteps. Each header is only one or two short lines; do not add a separate box behind it. Use a small time tick beneath the symbols, not a long global timeline that would require cross-canvas arrows.

Within every column the top input tier spans roughly y=139..298. Its left image field occupies the left 54% of that column, and its right field occupies the remaining 39%, leaving a narrow joining gutter. The left field is labeled “Recent history” and contains two slightly offset frames of the same tabletop. The front frame is fully visible; the rear frame is offset only enough to indicate a finite sequence. The right field is labeled “Fresh noise” and contains two small offset grain tiles corresponding to the new future frames. Do not overlay noise on the history field. The two input sources remain visibly separate until the channel-concatenation junction.

Put the symbols h_0, h_1, h_2 immediately under the respective history fields. Label each noise field “o_t^(K)” in compact math. Here t refers to that column's next interaction output and K to the high-noise end of diffusion; there is no numerical K on the drawing. The noise should look like a neutral grain sample, not an uncertainty heatmap, latent semantic feature, reward signal, or historical video. Keep the noise glyph large enough to be recognizable but smaller than the history image and much smaller than the generated output strip.

From the lower edge of each history field, draw a dark thin arrow down toward a small two-layer concatenation glyph centered under the joining gutter at y=314. From the lower edge of the fresh-noise field, draw a separate dark thin arrow into the other upper port of that same glyph. The glyph is two slightly offset planes, one bearing a tiny history-image miniature and the other a gray noise pattern, sharing a common edge. Label it “concat channels”. This is channelwise concatenation of conditioning frames and noisy future input, not temporal concatenation of finished video segments and not addition of the two pixel arrays.

The concatenation glyph's bottom port enters the top border of a compact operator labeled “Video diffusion”. This operator spans the right 57% of each column around y=350..438. Its interior contains a simple U-shaped arrangement of three modest feature-plane pairs, with a short down-then-up path and tiny labels “space” and “time” beside two internal exchanges. These are a compact representation of the source video U-Net's spatial and temporal processing, not a complete invented architecture. Do not label numbers of layers, hidden dimensions, attention heads, channels, or parameters.

Place the small static note “repeat denoising” immediately beside the right boundary of each Video diffusion operator. This note explains repeated reverse diffusion using the same learned model, without a fabricated step count. Do not draw an exterior cycle, bidirectional loop, or new input wire here. The output exits the operator's bottom border. Keep the operator modest: it occupies less area than the generated images and does not become a huge empty rectangular model bar.

At y=350..438, to the left of each denoiser, place the column's action representation. It is a short horizontal token strip with a small type glyph and a concise action label. Its right edge sends a colored arrow into a distinct left-side port of Video diffusion. The history/noise concatenation enters from above and the action enters from the side, so the three conditioning roles remain clear. Do not route action directly into the generated image or connect it to the noise tile. The action conditions the prediction model; it is not the target video or an externally executed robot command in this drawing.

COLUMN 1: CONTROL-TYPE ACTION

The first action strip is labeled “a_0: motor control”. Use a small gripper-axis icon followed by symbolic tokens “Δx” and “Δω”. The values are not specified; do not print coordinates, degrees, velocities, joint counts, or a calibrated axis grid. Show a second small adjoining segment titled “control values” with three narrow neutral blocks, representing the availability of discretized low-level controls alongside the language embedding. This is a visual description of the unified action interface, not the literal dimensionality of a robot controller.

Place the phrase “action embedding” beneath the first action strip. Its connecting arrow ends at the left boundary of the first Video diffusion operator. Use muted ochre for control-type action accents. The upper history field h_0 shows the initial approach scene. Its two offset frames may differ only in a tiny gripper approach motion, while cube and bowl identity remain fixed. The fresh noise field sits beside h_0, not between action and model. The model's bottom arrow leads to the first output strip with two fully visible images.

The output strip spans x=20..318,y=479..678 and is titled “Predicted segment o_1”. Divide it into two adjacent observation fields with a thin gutter, each filling its viewport with the table scene. The first frame shows the gripper approaching; the second shows it close to the cube's left side. Put a small unnumbered time arrow beneath the two frames from left to right. Do not write “successful grasp”, “task complete”, “real robot”, or any measured outcome. No checkmark or trophy belongs at this output.

At the output strip's lower-right edge attach one small circular marker labeled “H1”. A very short dark arrow runs from the final-frame border to H1. Beneath the strip, print “recent frames → next history” in one compact line. The matching H1 marker is attached at the upper-left edge of column 2's Recent history field, with a short inward arrow. These paired markers explicitly carry generated recent visual observations forward without a long diagonal over other scientific content. They are continuation ports, not new trainable modules or memory banks.

COLUMN 2: LANGUAGE ACTION

The second action strip is labeled “a_1: language”. Print the exact short text “push cube right” inside a light green token band. A tiny text-glyph square precedes the phrase. Immediately below, draw four narrow contiguous embedding blocks labeled “T5 embedding”. These blocks indicate a continuous text representation and do not imply that four is its true dimension. There is no extra proprietary language agent deciding the action inside the figure. This action is supplied to the simulator's unified interface.

The history field h_1 is the end of the first generated segment: the gripper is near the unmoved blue cube, which remains left of the bowl. Attach the H1 marker with a visible inward arrow at this exact history field. Do not attach it to Fresh noise, the action strip, or the denoising note. The front history frame repeats the end of o_1 with the same table perspective and object details. This precise reuse is more important than texture richness. The history's rear frame may represent one earlier recent observation from o_1 without introducing an unexplained time jump.

The second column's history/noise arrows meet only at its own concat channels glyph. Its action-embedding arrow enters only its own Video diffusion operator. The noise sample is fresh for this interaction, not a copy of the noise field from column 1. Use a small different grain arrangement without assigning it a numerical seed. The three columns use one shared learned model; their appearances are repeated calls, not different learned simulators. Print “same model” in small type beneath the central operator as a scope note, with no arrow joining the three model boxes.

The second output strip is “Predicted segment o_2” at x=352..650,y=479..678. Its first frame shows the gripper making contact with the cube; its final frame shows the cube moved toward the orange bowl while staying on the tabletop. The bowl and dark rear mat do not move. Use a small rightward motion guide beneath the cube in the first frame only if needed; it must not resemble a data connection to another column. The guide has no distance label. The small white cube mark and bowl-rim notch remain visible where orientation permits.

Attach an H2 continuation marker at o_2's final-frame lower-right edge with a short outward arrow. Attach the matching H2 marker at column 3's history field with a short inward arrow. The label “recent frames → next history” may appear under this strip just as under o_1, aligned at the same baseline. Do not let H1 or H2 look like an action token: use dark outlined circles, while actions are colored rectangular text strips. No one-way long wire should cross the generated frames to express these already explicit correspondences.

COLUMN 3: CAMERA-TYPE ACTION

The third action strip is labeled “a_2: camera”. Print “turn viewpoint right” in a pale violet token band with a small camera-axis icon. Use a curved camera-motion cue beside the icon, not a physical force acting on the cube. No particular rotation angle or zoom factor is specified, so do not add one. Beneath the token band, show the small “action embedding” block consistent with the other columns. The source handles actions extracted from camera motion; this drawing illustrates that action type without declaring a universal default control value.

The third history field h_2 copies the final state of o_2: the cube has moved closer to the bowl and the gripper remains beside it. It still uses the original fixed camera angle. The H2 marker's inward arrow touches the history frame boundary. This is important because the camera changes only in the next predicted segment, not before the action is supplied. Keep the fresh noise tile distinct and the concat channels glyph intact. The action arrow enters the side of the denoiser just as in the earlier two columns.

The third output strip at x=684..982,y=479..678 is titled “Predicted segment o_3”. The first frame begins near the prior viewpoint; the second presents a slightly right-shifted viewpoint. Draw the table as a mildly changed trapezoid, with the bowl, cube, gripper, and rear mat all reprojected consistently. The cube remains next to the same bowl rather than returning to its original far-left position. No world-object motion should be invented to explain the camera change. The scene remains a conceptual predicted video rather than a calibrated multi-view reconstruction.

Under this third strip place one compact continuation note “repeat with a new action”. A short arrow from o_3 can end in a tiny open continuation chevron at its lower-right edge, with no fourth empty scene or long loop back to the first column. The depicted model can be rolled out autoregressively; the figure shows three illustrative interactions, not a claim of an unbounded perfect simulator. Keep the column's bottom area populated by the concise conditional-interface key described next, not by a blank result panel or an illustrated reward score.

CONDITIONAL MODEL KEY AND SCOPE

Use the narrow y=704..752 line beneath the three outputs for one concise shared model statement: “p(o_t | h_t−1, a_t−1)”. Place it near the first-third boundary in readable mathematical text. To its right define “h: finite recent frames”, “a: temporally extended action”, and “o: predicted video segment” as three short, aligned phrases. The notation carries the source's exact conditioning distinction. It should not become a giant equation footer or a second title. These labels can occupy the natural lower edge of the main scene without creating another large card.

The action-time index t and the diffusion noise index k are different. Use superscript K only on the initial noisy future input and the words repeat denoising for the repeated internal steps. Do not put k on H1/H2 or imply that each diffusion step is another interaction with an agent. The same generated video can provide a finite set of recent frames for the next call; this is not an explicit persistent hidden-state memory module. No long all-history tensor, reward model, recurrent-state cache, or separate physical simulator should appear in the main pathway.

The diagram's top story is inference or conceptual rollout. It does not draw a sensor capturing a real robot in real time, execute a motor command on hardware, or claim that the simulated video has been tested. The gripper is a scene object inside the illustrative observation fields. Any small camera or gripper glyph on an action strip denotes an input action type, not a tool called by the generator. Keep these distinctions visible through placement and labels instead of adding a long disclaimer paragraph to the artwork.

SOURCE DIVERSITY AND TRAINING ROW

Below the thin separator at y=786, print the small heading “Training: diverse action–video pairs”. Place five tightly packed source visual cards across x=20..678,y=827..952. The cards have equal small image heights, modestly varied widths to fit their short labels, and no large outer container. Each card has one original miniature scene or frame strip above a short action-format cue. Their role is to show source diversity and unification, not to claim these exact images were in the training corpus.

The first source card is “Simulated scenes”. Draw a small rendered hallway frame with a doorway and a simple navigation path turn. Beneath it write “text + controls”. The second is “Robot videos”, showing the same generic type of gripper above a different neutral tabletop; beneath it write “tasks + controls”. The third is “Human activity”, with a simple hand moving a kitchen cup; beneath it write “labels → text”. The fourth is “Panoramas”, showing two adjacent crops of one room panorama and a small camera turn marker; beneath it write “camera motion”. The fifth is “Image–text”, showing one small person-walking scene with a caption strip; beneath it write “single-frame video”.

None of these source miniatures is a photograph copied from the paper. Use original simplified scenes with enough recognizable detail to distinguish the domains, but keep them subordinate to the main tabletop sequence. The five cards share a lower alignment baseline. Do not create an empty landscape image just to occupy a wide card; crop each miniature to its object and visual purpose. Do not print dataset brand names, logos, sample counts, or a map suggesting the model covers every domain on Earth.

Under the five source cards, retain the thin bracket labeled "unified action / video interface". Preserve the existing training footprint to its right. In that lower-right region draw a compact local construction with THREE separate visual operands: a clean tabletop target thumbnail labeled "clean video", a neutral grain tile labeled "sampled noise", and a visibly grain-corrupted tabletop thumbnail labeled "noisy video". Put clean video above sampled noise in the leftmost narrow stack. Place a small operator labeled "Noising" just to their right. Each input has its own short arrow touching a different left input port of Noising. Its right output goes to the distinct noisy-video thumbnail, which then goes to the left input of Train video denoiser. The noising operation represents the source's noise-level-dependent corruption, not unweighted addition. Neither an input nor its label may double as the noisy-video result.

Train video denoiser has exactly ONE outgoing data arrow, from its right border to one grain glyph labeled "predicted noise". From that glyph, one short arrow enters MSE's left port. The sampled-noise tile supplies the OTHER loss operand: branch at that original tile's boundary, with one route into Noising and a separate short downward arrow into a circular emitting N. Put N immediately beneath the sampled-noise source, aligned to its center, far left of the noisy-video result. Place a matching N directly below MSE, with a short upward arrow into MSE's bottom port and the note "same sampled noise" beside it. No line starts at Noising to reach either N. MSE has exactly two incoming operands: predicted noise and the original sampled noise conveyed by N. Do not add a second prediction, a second model output, or any edge from clean video or noisy video into MSE. Use the static note "learn denoiser parameters" below the model, without a gradient loop. MSE is the training loss, never a measured result or score.

Keep the conditioning inputs separate from both training target and corruption noise. Above the compact denoiser, retain one small recent-history thumbnail labeled h, a "T5 text" tab, and a "controls if available" tab. Each has a distinct short downward or right-angle arrow into a separate top port of the denoiser. Move only these local tabs if needed to fit; none enters Noising, either noise operand, or MSE. Text embeddings and available discretized controls form the source action interface; low-level controls are optional across source domains. The h thumbnail depicts past frames, while clean video depicts the future training target. Keep the existing static "shared trained model" relationship in the main key. There is no training-to-inference data wire or privileged target entering a main column.

Within the existing training region, reserve a shallow top tier for h and action conditions. Across the middle tier, read left to right: stacked clean video / sampled noise, Noising, noisy video, denoiser, predicted noise, MSE. The two N circles sit below their sampled-noise and MSE anchors, with vertical links detached from Noising's output. Use a small two-line Train video denoiser operator with NO internal feature-plane stack to make space for real operands. The noisy-video glyph has scene structure and grain; the sampled/predicted noise glyphs are pure grain. Label each object once. Preserve the footprint, lower alignment, source-card border, and tight white canvas. Add no panel or blank space.

The training scope stops at the thin separator. Do not add an external critic, reward-learning stage, planning tree, policy optimization loop, live robot execution arrow, sound channel, or downstream transfer-result panel. UniSim's paper studies additional downstream uses, but this overview's role is the unified action-conditioned observation model. Those applications are omitted here for a clear source-grounded scope, not because the method lacks them. The figure remains complete at the specified input-model-output-and-training level.

VISUAL GRAMMAR AND EXACT CONNECTION RULES

Use charcoal #29343E for text, frame outlines, and the main data arrows. Use restrained ochre #B88A38 for motor-control tokens, muted green #5C926B for language tokens, and muted violet #806CA5 for camera tokens. Keep the conceptual blue cube and orange bowl colors constant regardless of action type; object identity must not inherit a module's semantic color. Fresh-noise tiles are neutral gray. History and generated image frames have thin dark borders without broad pastel fill around them. Each scientific viewport is atomic in appearance, with independent frame, label, and connector.

The inter-column H1/H2 ports and their local arrows remain frozen: H1 starts at the last frame of o_1 and enters h_1; H2 starts at the last frame of o_2 and enters h_2. Neither H1 nor H2 attaches to noise or action tokens. The separate training-only N pair transports the original sampled noise from its source tile to MSE, with its own short source and receiver arrows. N has no relation to H1/H2. Each main history and fresh-noise field still enters its own concat channels glyph; that glyph enters its denoiser; each action strip enters the denoiser separately; each denoiser enters its predicted output strip. Preserve Interaction 2's existing downward output arrow and keep its same model note clear. There are no arrows between action strips.

Use primary data arrows at roughly 1.0–1.2 pt at the intended printed width, secondary local dependencies at about 0.7–0.8 pt, and short small arrowheads that touch actual object borders without covering image content. Avoid lines that disappear behind a viewport or an action label. The denoising note has no arrow; it is a description of iteration internal to the operator. A necessary crossing uses a visible bridge or separated lane, never a junction dot unless the signal truly branches. Keep all action ports on the same side of their corresponding repeated operators to make the conditional structure comparable.

A static same model label sits beside the second denoiser, clear of its bottom output arrow, but do not draw a model-to-model flow that falsely serializes the simulator weights. The columns are sequential because their generated observations form the next history, not because one denoiser sends its parameters into the next. The training row similarly has no edge directly entering the main history or output fields. Its data pairs produce learned parameters; the input/output contract at inference remains h and a to o.

Ensure that image contents remain aligned with labels. Every field titled Recent history contains a previously available or sampled scene. Every field titled Fresh noise contains grain for a new future sample. Every Predicted segment contains newly illustrated consequences of the supplied action. Keep the first two cameras identical, and reserve the viewpoint change for the third camera-action output. The concrete sequence has no identity switches: the Q-style label from another example does not appear here; the main objects are only the blue cube, orange bowl, white gripper, rear mat, and tabletop.

Type only the named scientific labels, the three action texts, the compact notation and definitions, the five source-domain labels and format cues, the training operators/operands, and Conceptual illustration. Do not print construction coordinates, aspect-ratio percentages, region labels from this prompt, paper page numbers, source URLs, model costs, approval notes, density thresholds, source hashes, or descriptions of what the illustrator should do. The artwork must look like a scientific method figure, not a production checklist or a poster of prompt instructions.

DENSITY AND OUTPUT REQUIREMENTS

Keep the three action-conditioned output strips large and immediately readable. The upper history/noise pairs and action-model interfaces should fill their tiers with relevant content; align their bottoms so the arrows are short. The five source miniatures and the local training operands should occupy the lower row with little wasted height. Do not add broad whitespace above or below the conditional-model key, and do not leave a blank right-hand corner below o_3. Use the existing key, source scenes, and training operands to close local gaps without adding unrelated modules.

A smooth region inside a meaningful tabletop, protein-free scene, room, or video field is scientific image content; do not artificially texture it to score as ink. Conversely, a large empty card containing a tiny label is not meaningful density. Tighten the crop or enlarge the actual in-scope observation fields before introducing more writing. Preserve adequate short connector lanes and the h/noise/action distinction. Final review will judge actual object occupancy and the largest compressible empty regions, not the amount of colored background or raw dark-pixel coverage.

Render one complete original conceptual raster overview. Use the source's unified action-conditioning and finite-history relationships faithfully, with no invented scientific results. The output is a visual explanation of the conditional simulator and its training inputs. It is not a live simulation, video benchmark, real robot record, or an assertion of universal physical validity. Keep that boundary through the small Conceptual illustration label and the scientific scene labels, while the visual itself remains compact, concrete, and readable.
