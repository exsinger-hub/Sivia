Edit the supplied VisProg v2 image once for denser use of space while preserving all scientific operations, program lines, true image identities, correct SELECT colors and final COLORPOP semantics. The substantial change is physical size: enlarge the four central image/object fields vertically, and enlarge all lower application pictures. Reclaim space by removing the large interpreter footer, its wide Visual rationale bracket and sentence-only band, and by reducing unused space below the lower applications. Place the original IMAGE bypass in a very narrow lane immediately beneath its carriers, with a source stem touching the original image frame. Keep the lower applications independent. The result should be a visibly tighter, more image-dominated version of the supplied figure, not an unchanged image with extra decorative fill. Do not add modules, duplicate examples, empirical results, or longer prose. Produce one dense, publication-quality scientific illustration titled "Visual Programs, Visible Steps". This is a source-grounded conceptual overview of VisProg, the method in Visual Programming: Compositional Visual Reasoning Without Training, CVPR 2023. Use case: scientific-educational. Make a new illustration with new example scenes, not a copy of a paper page or a screenshot montage. The small text "Conceptual illustration" must be visible directly beneath the title. The central visual claim is that an instruction can be compiled into a short program whose successive calls to existing visual and symbolic modules produce inspectable intermediate objects and a final response or edited image. Every depicted example is illustrative; none is a new measured execution of the paper's system.

SCIENTIFIC SCOPE AND READING

Show the separation between program generation and visual execution. The language model receives a natural-language instruction and a few instruction–program examples; it produces a program. The source method uses GPT-3 for this step. It does not inspect the target image while generating the program. The image is supplied to the program interpreter, where the generated module calls operate on images, object lists, masks, and other returned values. Intermediate results can be displayed as a visual rationale. Existing trained models and ordinary image-processing or symbolic functions supply the modules; the figure does not imply that all of these functions are untrained.

At first glance, the reader should recognize a single colorful tabletop image becoming a segmentation, a selected object, and an edited image through three compact program steps. At working distance the reader should see the separate instruction-to-program route above that execution strip. At close distance the reader can align the three program lines with the three execution stages and track the original image into the final color operation. Two compact applications below demonstrate that the same program-and-execution idea can produce a count or a replacement image. They are not later stages of the tabletop example.

The dominant scene concerns one instruction: "Keep only the red mug in color." Its purpose is to expose the difference between an image, a set of segmented objects, the selected object, and an image transformation conditioned on that selected object. The lower applications are subordinate, not a performance gallery. Do not introduce a verifier, repair loop, training loss, teacher, confidence gate, benchmark plot, reward, or evaluation dashboard. Preserve the source method's visible program variables and module composition without inventing a new decision system.

CANVAS, SPATIAL BUDGET, AND DENSITY

Use a 4:3 landscape composition at high raster resolution, intended for an approximately 180 mm wide scientific figure. The background is white. Keep the outer margin near two percent of canvas width and avoid a large masthead. The title occupies a compact strip. Keep the existing instruction-and-program band readable, then use nearly all remaining vertical space for the enlarged interpreter image fields and lower application pictures. Reclaim the former explanation-only footer and broad blank lanes for these scientific viewports. Maintain narrow outer margins and gutters. There is no additional explanatory footer below the images, and these layout instructions are never printed as artwork text.

The meaningful-content hull should approach all four inner edges. Use real pictured objects, substantial mask fields, program tokens, and corresponding before/after images to occupy the space. Large flat-color containers, ornamental shapes, and decorative title flourishes do not count as useful content. Each image field must be large enough to see the mug handle, object boundaries, and preserved background. If space remains within a scene, enlarge those fields or bring their neighboring operators closer; do not leave a blank card interior or add an unrelated module to fill it.

Organize the top control band left to right: instruction and examples, program generator, three-line program. Organize the central execution band left to right: original image, all-object segmentation, selected red mug, color-pop output. Give the four main visual fields similar height while allowing the selected-mask field to be slightly narrower. Put the segmentation, selection, and color-pop module labels immediately above their corresponding output fields. The lower pair each has its own task heading, input picture, short module sequence, and answer or output picture. Thin dividers and local labels establish groups without large framed panels.

Keep all main image top edges aligned and their lower caption baselines aligned. Each central image field should use almost its full allocated cell, with only the space needed for one brief label and the connecting arrow. Relative to the supplied image, grow the central photographic/segmentation fields by a visible amount, approximately a further mug-handle height vertically, and scale their depicted objects up consistently. Crop closely around the same four objects; do not create a large blank wall or tabletop in the added viewport area. Maintain a narrow, consistent gutter between fields. Avoid widely separated floating diagrams or a single huge blank arrow corridor. The original-image bypass has one tightly fitted thin lane directly below its source and consumer representations. Keep only the short label original IMAGE and the line itself in this lane. Remove the extra whitespace above and below the former rationale bracket. The bypass source stem physically touches the original image bottom border, its branch dot lies on that boundary, and its destination remains the original-image input of COLORPOP. The bypass does not pass through SEG or SELECT.

TITLE AND PRINTED TEXT

Print "Visual Programs, Visible Steps" in a compact bold technical sans serif, with no brushstroke underline, logo, author block, conference badge, or external citation inside the artwork. Beneath it print "Conceptual illustration" in small readable dark gray type. A short subtitle on the same top band may read "Instruction → program → inspectable execution". This sentence is the only long title-region line. All other production prose in this prompt is drawing guidance, not in-figure text.

At final publication width, aim for 13–15 pt title, 9–10 pt scene headings, 8–9 pt operator labels, and at least 7.5 pt program and annotation text. Use a clear monospaced font for the three illustrative program lines and code fragments; use clean sans serif elsewhere. Do not simulate tiny unreadable code by filling blocks with random glyphs. Real printed code is limited to the explicitly listed lines. Line breaks belong between program statements, not inside variable names or function names.

TOP LEFT: INSTRUCTION AND EXAMPLE PAIRS

Place a pale ivory instruction slip in the left thirty-six percent of the upper band. Its heading is "Instruction" and its single sentence is "Keep only the red mug in color." The slip is a natural-language input, not a generated answer. Under it, show a short stack of two smaller, slightly offset white example slips under the heading "Instruction–program examples". These slips establish few-shot prompting without consuming the main figure with a long prompt transcript.

On the first example slip print "Find the blue cup" and the compact fragment "LOC → SELECT". On the second print "Count the balls" and "LOC → COUNT". These are schematic demonstrations of instruction-to-module composition; they do not prescribe a universal program for every instruction. A small brace beside the slips may group them as examples, but the slips are distinct from the new instruction. Do not connect either example to the actual tabletop image or to any output mask. Their only outgoing direction leads into the language-model generator.

Draw two short charcoal arrows from the new instruction and example stack into the left edge of one compact lavender operator labelled "Program generator" with "GPT-3" on its second line. The input arrows have separate source anchors and single heads touching the generator boundary. The instruction and examples are parallel prompt operands, not two serial processing stages. Their visual spacing should make the distinction clear without additional prose. The generator is smaller than the combined example images in the execution scene.

The generator can be a low rectangular operator containing two small stacked token bars, indicating language processing. Do not draw a brain, robot, cloud service, conversational face, or oversized server. A single charcoal arrow leaves its right side and enters the left edge of a white program sheet. There are no image-colored input arrows at the generator. No line travels from a tabletop image into GPT-3. This is an interpretation-changing boundary and must remain readable even in grayscale.

TOP RIGHT: THE THREE-LINE PROGRAM

The program sheet fills the remaining upper-band width and has the heading "Illustrative program". Print exactly three monospaced statements on separate lines:
"OBJ0 = SEG(IMAGE)"
"OBJ1 = SELECT(OBJ0, 'red mug')"
"OUT = COLORPOP(IMAGE, OBJ1)"
These are concise illustrative pseudocode calls, not a claim of a byte-for-byte software interface or an executed trace. Keep all variable names consistent. Use a tiny amber numbered disk beside each line: 1, 2, and 3. These numbers enumerate the displayed program statements only; they are not trial indices or performance values.

Highlight SEG, SELECT, and COLORPOP with the same amber accent used for the corresponding execution-stage headings. Underline OBJ0 and OBJ1 with distinct fine blue and coral marks to make data reuse recoverable. In the third line, IMAGE and OBJ1 are both present because the image operation needs the original image and the selected object. Do not simplify the last statement to a mask-only color operation. The sheet remains flat with three rows and enough padding for each statement; avoid a code-editor toolbar, syntax line-number gutter, or scrollbars.

One short downward charcoal arrow leaves the program sheet's bottom edge and enters the heading of the central execution group labelled "Program interpreter". This is the program-control connection, not a tensor connection to a particular image. Place the interpreter heading above the three operator labels and cover only their intended group. The separate source image enters the first step through its own direct image-data arrow. Do not draw a second generator or a duplicate execution engine in the lower applications.

CENTRAL LEFT: THE ORIGINAL TABLETOP SCENE

The original input is a carefully drawn, realistic but clearly illustrative tabletop photograph-like field. Use an oblique view looking slightly downward onto a light wooden table in front of a pale wall. The same camera view, lighting direction, crop, and object placement will appear in the final output. The field has a wide rectangular crop with a subtle gray boundary. Print "IMAGE" above the field and "Input image" immediately below it.

Arrange four clearly separated objects with different colors and silhouettes: a bright red ceramic mug at front left, a blue ceramic cup at middle right, a green pear behind the mug near center, and a small yellow closed notebook lying toward the back right. The red mug has one open handle pointing left, a visible dark interior ellipse, and a gentle shadow cast backward right. The blue cup is shorter, without a mug-like handle, so the query cannot confuse the two vessels. The pear has a narrow stem and curved body. The notebook is a thin rectangular object, not another cup.

Use enough local texture to explain which pixels belong to surfaces: understated wood grain, ceramic highlights, a notebook edge, and soft contact shadows. Do not fill the image with visual clutter, text on the notebook, extra utensils, or additional red objects. All four objects fit fully within the frame with a little breathing room. None is cut off or touching another; segmentation boundaries can then be shown without introducing an occlusion-solving claim. Their spatial ordering stays identical across all central fields.

A short solid blue-gray arrow goes from the original image's right boundary into stage 1, labelled "SEG" above the all-object segmentation field. Its arrowhead touches that stage's actual image or operator boundary. A separate branch from the original image's bottom edge follows the narrow lane below the execution strip and enters the original-image input of the final COLORPOP operation. Label this thin route "original IMAGE" once, near the midpoint. Put the single branch dot directly on the original IMAGE frame bottom border, with a short uninterrupted vertical shaft down to the bypass lane. The shaft must touch both the frame and the lane; no floating dot or white gap is allowed. Use no junction dots where the route passes other fields.

STAGE 1: SEGMENT INTO OBJECT CARRIERS

Stage 1 has the small numbered disk 1 and the operator label "SEG". Its main visual carrier is the same tabletop crop overlaid with four clean semi-transparent masks, one for each of the four objects. Red-mug pixels receive a coral overlay, blue-cup pixels a blue overlay, pear pixels a green overlay, and notebook pixels a mustard overlay. Retain a faint grayscale version of the underlying scene so object shapes and positions remain identifiable. Mask colors are object-identity markers, not class probabilities or measured activations.

The masks follow actual silhouettes. The mug mask includes the handle ceramic but excludes the empty handle hole and the background visible through it. The inside visible ceramic rim remains part of the mug, while the surrounding table remains unmasked. The cup boundary is separate from the mug. The pear stem is included with the pear; the notebook mask is rectangular with the same perspective as its source. Keep the four masks disjoint because the input objects were deliberately separated.

Below this field show a short horizontal object-list carrier labelled "OBJ0: object list". It contains four small outlined thumbnails in the same order as the objects are described: mug, cup, pear, notebook. Each thumbnail carries the same identity color as its mask. The list is produced by SEG, not supplied as a separate ground-truth annotation. The visualization is an illustrative intermediate. Do not attach accuracy percentages, IoU scores, confidence numbers, or dataset labels to these thumbnails.

A short amber data arrow leaves the object-list carrier's right edge and enters stage 2 at its list-input edge. Label the route "OBJ0" once. The arrow represents the returned object list, not raw pixels or a textual explanation. The input image bypass runs below this list and stays visibly separate from it. This separation helps explain why the final edit requires both original image content and an object selection. Avoid drawing one common bus that merges the two data types early.

STAGE 2: SELECT THE QUERIED OBJECT

Stage 2 has the disk 2 and heading "SELECT". Show a small query chip directly above its field reading "red mug". This chip corresponds to the literal phrase in the program and is consumed by SELECT. A short downward arrow from the chip ends at the SELECT heading or its narrow operator bar. It does not point into SEG, the generator, or the final output. No additional language model is depicted for this selection step.

Within the SELECT field, retain the original natural-color tabletop photograph: the mug is red, the cup is blue, the pear is green, and the notebook is yellow. Preserve the natural table and wall colors. Do not desaturate or recolor any nonselected object in this field. Add only a fine bright red contour along the mug silhouette, including the handle boundary but excluding its hole, to visualize the selected object support. All original colors remain visible through this unfilled contour. The small selected-mug thumbnail below the field is the returned object carrier, labelled "OBJ1: red mug". The full-color field with its contour is a display of selection; actual grayscale conversion occurs solely inside the following COLORPOP output. This distinction must be immediately visible side by side.

Show the four tiny object thumbnails from OBJ0 above the selected field as a compact row, with the mug outlined strongly and the other three outlined lightly. A short inward selection arrow ends at the mug thumbnail. Only the mug is selected. Do not add a checkmark on every object or imply that the input was red-only from the start. The selected mug thumbnail and the full selected silhouette are two displays of the same returned OBJ1, not two separately computed decisions.

A solid coral data arrow leaves stage 2's right side and enters the mask/object input of stage 3 COLORPOP. Label that route "OBJ1". The original IMAGE bypass enters COLORPOP through a second, lower input. Make these two incoming endpoints visibly distinct and bring both to the operation before drawing its output field. Do not connect the mask directly to the final photo without showing its consumer. This is the only main two-operand merge in the execution scene.

STAGE 3: APPLY THE IMAGE OPERATION

Stage 3 has disk 3 and heading "COLORPOP". Draw a slim amber operator bar just above the output image. The selected-object arrow reaches its left edge; the original IMAGE bypass rises along its lower-left side and ends at its lower edge. One short downward arrow leaves the bar and touches the output image's top boundary. This small, explicit junction shows where the chosen mask and original image are used together. Keep the source labels adjacent to their own incoming routes.

The output uses the exact same tabletop view, crop, object geometry, shadows, and positions as the original IMAGE. Only the red mug remains in full original color. The blue cup, pear, yellow notebook, table, and wall become neutral grayscale. The mug remains red rather than acquiring the symbolic coral mask fill; retain the original ceramic shading and highlights. The handle opening reveals grayscale background. Do not erase, move, resize, or replace any object in this example.

Label this final field "OUT: color-pop image". The visual operation should be apparent without reading the label: one red vessel remains colored against the otherwise grayscale scene. The output is not a reconstructed scene, a new object detector result, or a ground-truth target. Keep all source objects visible. Do not place an explanation sentence below the interpreter strip. The inspectable intermediate values are already evident from their labels and program variables. Do not add a supported/verified badge or a claim that the explanation guarantees correctness.

VISUAL RATIONALE AND VARIABLE CORRESPONDENCE

Print the compact label "Program interpreter · visual rationale" at the interpreter heading. Do not draw a wide grouping bracket under the three output carriers and do not reserve a separate rationale footer. This grouping text is enough to identify the rendered intermediate values while leaving more height for their real image fields. The original input remains labelled IMAGE and distinguishable from produced values.

Use variable labels to establish identity between code and images. OBJ0 appears on the program line that creates it and the list carrier that contains it; OBJ1 appears on its creation line, selected-object carrier, and the route to COLORPOP. IMAGE appears at the original field and its retained bypass. OUT appears only after the final operation. The same word must not silently refer to a program string in one place and a different image elsewhere. Extra independent object IDs are unnecessary.

BOTTOM LEFT APPLICATION: A COUNT RESPONSE

The left bottom scene is headed "Number output". It is a separate compact illustrative application, framed by a single thin top rule and no large colored background. Place a substantially enlarged input field showing exactly three blue balls and two orange cubes on a light neutral surface. Increase its height and that of the adjacent LOC view using the space reclaimed from the old broad interpreter footer and bottom padding. The objects should occupy the added area through a tighter, larger crop, not through extra empty background. All five objects are separated and fully visible. Blue spheres have round silhouettes; orange cubes have visible flat faces and corners. The instruction below the field is "How many blue balls?".

Draw a short rightward arrow whose tail touches the counting input image right border and whose single head touches the LOC image left border. The input and its localized result must have a visibly complete directed connection. Next to that input show a narrow localized intermediate: three blue outlined boxes around the balls on a faint copy of the same crop. Label it "LOC: blue balls". The boxes are conceptual returned regions, not measured detections. A short arrow from this region carrier enters a compact COUNT operator. A final short arrow enters a small white answer card containing "3" and the label "count". The numeral is the count of explicitly drawn objects in this synthetic scene, not a benchmark value or accuracy statistic.

Use the compact printed module sequence "LOC → COUNT" once across the top of this lower application. Do not add an extra classifier, detector-confidence threshold, or text generator. The pictorial objects make the count checkable. Preserve the number of balls across the input and localized view; the orange cubes remain in the image but have no blue localization boxes. Keep the answer card much smaller than the image fields so this application remains visual rather than a giant number tile.

BOTTOM RIGHT APPLICATION: A LOCAL REPLACEMENT

The right bottom scene is headed "Image output". Its instruction is "Replace the blue cube with a wooden block." Show an enlarged original still life with one blue cube beside an orange bowl on a neutral surface. The input, selected-object carrier and final replacement view should be visibly taller than in the supplied figure, aligned with the enlarged counting photos. Grow the depicted objects consistently and crop closely; do not add new objects or blank ground to fill the added height. To its right, place a small selected cube-mask carrier and then a paired output image. In the output the cube's region contains a small wooden block of comparable size, with the orange bowl and the surrounding scene preserved. This is an illustrative edit, not an actual evaluation sample.

Above the mask and output print the sequence "SEG → SELECT → REPLACE". Connect the original picture into the segmentation/selection part, and connect the selected mask into the local REPLACE operation. A second short lower branch from the original picture enters REPLACE so the scene context is present. Keep this application compact enough that its simple two-input local edit remains readable. The wooden block should not appear at a different position, become a large building, or change the bowl; the transformation is spatially restricted.

Use a fine blue boundary for image fields and an amber caption for module calls, matching the central interpreter. No arrow links the counting scene to the replacement scene or either application to the main mug output. They demonstrate alternative programs under the same method. Do not make the two lower scenes a serial pipeline. Separate them by one modest vertical gutter, not a broad empty column.

SOURCE-FAITHFUL INTERFACES AND OMISSIONS

The only language-model operator in this figure is the top Program generator. Module internals are intentionally abstracted because the overview explains composition and visible intermediate values. SEG, SELECT, COLORPOP, LOC, COUNT, and REPLACE are recognizable module roles grounded in the source's supported visual and symbolic operations. Their specific implementation networks, internal parameters, and training histories are not expanded. No empirical comparison panel or training flow is needed for the chosen claim.

The three-line program is concise illustrative pseudocode. Do not add import statements, API calls, file paths, assertions, try/except blocks, dataset commands, hidden prompts, or functions absent from the scene. The purpose is to let a reader follow variable flow, not to advertise a new executable package. Program numbers correspond to the displayed steps. The lower count numeral is grounded solely in the explicit conceptual objects. No other numerical metric belongs anywhere in this figure.

VISUAL LANGUAGE AND MATERIAL

Use a restrained editorial scientific style combining flat technical vectors with carefully shaded concept image fields. Program generation is lavender; executable module labels are amber; image and object carriers use blue-gray outlines; selected mug identity uses coral; final image content retains natural material colors. Color coding must support shape and printed labels, so the data types remain distinguishable without hue. The grayscale version should still reveal the original image field, object-list carrier, selected mask, and final image.

Keep outlines charcoal at a consistent weight. Main data arrows are slightly heavier than explanatory brackets; the program-control arrow is charcoal; the original-image bypass is a thinner blue-gray line. Use single arrowheads on data and control flows. Any remaining local grouping line has no arrowhead; the former wide interpreter footer bracket is absent. Arrow shafts should stop at actual object boundaries, without floating gaps, reverse tips, or heads hidden beneath labels. Never use a two-headed arrow merely to mean that two operands are present.

The central image fields contain enough detail to occupy their rectangles, but external whitespace stays plain and purposeful. Avoid full-page pale cards, giant soft shadows, embossed operator blocks, circuit-board textures, decorative robots, or arbitrary dots. A soft contact shadow inside the tabletop illustrations is part of the depicted scene; a large panel shadow is decoration and should not be used to increase density. Keep operators flat and small relative to what they transform.

LAYERING, LOCAL FIT, AND FINAL CHECK

Draw scene fields first, then masks and object carriers, then operator bars and connectors, then labels. Mask overlays align to the pictured objects rather than covering captions. Connector lanes stay outside photos except where a box or selection overlay intentionally identifies an object. Label backing may be white locally, but it must not erase a junction or an arrowhead. Tiny object-list thumbnails should remain separate from the larger spatial mask fields, maintaining their different representation roles.

Check that the new instruction and the examples enter GPT-3, that the target image enters the interpreter only, and that the generated program has exactly the three main statements. Follow IMAGE into SEG and into COLORPOP; follow OBJ0 from SEG into SELECT; follow OBJ1 from SELECT into COLORPOP; follow the operation into OUT. Inspect the mug handle hole and the grayscale treatment of every non-mug object. Then independently check the three-ball count and the local cube replacement. These checks concern the conceptual figure, not empirical model performance.

The complete artwork should feel like a tightly arranged set of scientific scenes, with large visible correspondences and short technical labels. All substantial whitespace must separate representations, protect text, or carry a specific connector. The lower picture captions end near the bottom margin. No broad white band remains beneath COUNT and its answer, and no wide sentence-only band separates the interpreter from the applications. The count answer card stays compact; enlarge the scientific image fields into available area instead of enlarging a blank numeral card. Render a single finished raster concept figure with its title and "Conceptual illustration" marker, no page number, no paper-page border, no production notes, and no claim of validation, admission, or measured success.
