Design one publication-ready research overview figure inspired by the paper 'Dense-SfM: Structure from Motion with Dense Consistent Matching'. This is an original, source-grounded redraw for a Sivia knowledge-base example, not a reproduction of the publisher's artwork and not a scientific result. Domain: dense multi-view reconstruction and feature tracking. Core claim: Dense matching, Gaussian-splatting track extension and a multi-view kernelized refinement module produce long, consistent tracks for accurate structure from motion.

The source is a recent top-conference paper with a method overview that should read as a compact visual argument: multi-view images → dense matching → GS-based track extension → kernelized refinement → consistent 3D reconstruction. Use one 16:9 white-paper composition and one dominant causal path: input → representation or module → transformation → output/use. Use crisp flat editorial vector linework, small meaningful thumbnails, deliberate alignment, charcoal text and at most three accent colours. Keep the content hull compact.

Do not use gradients, glow, drop shadows, glossy 3-D blobs, photorealistic robots, rounded dashboard cards, oversized title banners, rainbow palettes, decorative icons, fake benchmark charts, invented numbers or generic AI pipeline boxes. Draw only the source-grounded objects below, connect every arrow to a visible start and end, and make the representation change explicit. Use short labels and consistent stroke weights; the figure must read as an authored conference overview rather than an AI infographic.

Build these source-grounded elements:
1. A set of multi-view images with dense correspondences shown as sparse coloured tracks across repeated scene points.
2. A Gaussian-splatting track-extension stage that lengthens the correspondences into a consistent multi-view graph.
3. A kernelized matching/refinement module that rejects inconsistent edges and returns a cleaned track set.
4. A final camera-and-point reconstruction with the long tracks visibly supporting the recovered 3D scene.

Use only these labels when needed: multi-view images, dense matching, track extension, Gaussian splatting, kernelized refinement, consistent tracks, 3D reconstruction. Do not invent architectures, losses, datasets, performance values, sensors or downstream tasks beyond the source note. Show qualitative results as relations or state changes, never fabricated plots. Add a small footer reading 'conceptual illustration; source-grounded method'. Check thumbnail legibility, low whitespace, balanced ink density, clear hierarchy, unambiguous arrows and one coherent overview narrative.