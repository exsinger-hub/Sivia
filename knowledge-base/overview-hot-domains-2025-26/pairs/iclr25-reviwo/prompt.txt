Design one publication-ready research overview figure inspired by the paper 'Learning View-invariant World Models for Visual Robotic Manipulation'. This is an original, source-grounded redraw for a Sivia knowledge-base example, not a reproduction of the publisher's artwork and not a scientific result. Domain: view-invariant representation learning for robot control. Core claim: ReViWo separates view-invariant and view-dependent representations, uses the invariant state for a world model and policy, and improves manipulation under camera viewpoint disturbance.

The source is a recent top-conference paper with a method overview that should read as a compact visual argument: multi-view images → invariant/view-dependent encoders → target-view reconstruction → world model → policy. Use one 16:9 white-paper composition and one dominant causal path: input → representation or module → transformation → output/use. Use crisp flat editorial vector linework, small meaningful thumbnails, deliberate alignment, charcoal text and at most three accent colours. Keep the content hull compact.

Do not use gradients, glow, drop shadows, glossy 3-D blobs, photorealistic robots, rounded dashboard cards, oversized title banners, rainbow palettes, decorative icons, fake benchmark charts, invented numbers or generic AI pipeline boxes. Draw only the source-grounded objects below, connect every arrow to a visible start and end, and make the representation change explicit. Use short labels and consistent stroke weights; the figure must read as an authored conference overview rather than an AI infographic.

Build these source-grounded elements:
1. The same tabletop manipulation state observed from two camera viewpoints, with the shared task state marked but no photorealistic robot render.
2. Two encoders producing a view-invariant representation and a view-dependent representation, connected to a decoder that reconstructs a target view.
3. A world-model block consuming the invariant state and predicting the next manipulation state, with the temporal arrow explicit.
4. A policy selecting an action for a simple robot arm under a novel camera viewpoint, visually closing the loop.

Use only these labels when needed: viewpoint A, viewpoint B, view-invariant, view-dependent, decoder, world model, policy. Do not invent architectures, losses, datasets, performance values, sensors or downstream tasks beyond the source note. Show qualitative results as relations or state changes, never fabricated plots. Add a small footer reading 'conceptual illustration; source-grounded method'. Check thumbnail legibility, low whitespace, balanced ink density, clear hierarchy, unambiguous arrows and one coherent overview narrative.