from pathlib import Path
ROOT=Path(__file__).resolve().parents[3]; PAIRS=ROOT/'knowledge-base/restart-2026/pairs'
a=PAIRS/'autotool'
s=(a/'prompt.v3.txt').read_text(encoding='utf-8')
s=s[s.index('Create one complete scientific method illustration'):]
header='''EDIT THE ATTACHED AUTOTOOL THIRD DRAFT. Keep the compact right-hand two-route decision and its correct shared execution junction. Make only four local changes. DELETE the short horizontal arrow currently running from the bottom-center filled LoadAuthorNet card (Candidate + argument status) directly into Observation at bottom-right. Leave NO connector from that bottom-center card to Observation; delete its Candidate + argument status label as well. The decision inset already states the two conditions, so repeated notation is sufficient and no replacement wire is needed. Replace the right inset heading “Two-route decision (only place with LLM)” with the exact short heading “Two-route decision”; do not print internal layout instructions. Replace the bottom-right Status Success text with “Status: returned”. Finally redirect the leftmost Next decision feedback endpoint to Current context, not Goal: terminate it on the left edge of the Current context panel at its vertical center, with a short rightward arrowhead. Preserve everything else, including graph and parameter details. The attached AutoTool image is the edit target. The full scientific specification follows; existing correct composition is locked.\n\n'''
(a/'prompt.txt').write_text(header+s,encoding='utf-8')

a=PAIRS/'sigmadock'
s=(a/'prompt.v1.txt').read_text(encoding='utf-8')
s=s.replace('Use the attached approved DiffDock illustration as a visual reference','Use the historical approved DiffDock illustration as a visual reference')
s='\n\n'.join(p for p in s.split('\n\n') if not p.startswith('The scope is a mechanism illustration,') and not p.startswith('The pocket is known and fixed in this method setting.') and not p.startswith('Use an unarrowed brace around the library'))
header='''EDIT THE ATTACHED FIRST SIGMADOCK DRAFT. Preserve all upper molecular fields and the correctly labeled Translation scores, Rotation scores and Reverse step boxes. Do NOT merge or rename those boxes. Add only the tiny static note “uses z_t” immediately below Reverse step. Delete the left-pointing arrowhead near t=T; the Reverse diffusion arrow points only right toward t=0. REPLACE the two lower geometric diagrams with clear four-node constructions. This is the critical repair: the old orange A-to-D line is WRONG, even though it is labeled d_AC. Erase that entire old top orange line and its label. Erase both old bottom violet lines and their labels. In the LEFT diagram use atom centers A=(455,858), B=(548,944), C=(690,944), D=(801,884) in the supplied 1448×1086 image. Draw a new orange dashed diagonal DIRECTLY from A at (455,858) to C at (690,944), label d_AC near its MIDPOINT. Draw a new violet dashed diagonal DIRECTLY from B at (548,944) to D at (801,884), label d_BD near its MIDPOINT. Keep solid A–B, B–C, C–D only. Remove the extra unlabeled purple sphere below D. For the RIGHT diagram use A=(970,862), B=(1057,940), C=(1187,940), D=(1315,901), and draw exactly A–C orange and B–D violet by those endpoints. The ABC plane is bounded by A,B,C; the BCD plane is bounded by B,C,D, sharing B–C. No orange A–D edge, no violet line to a dangling atom, and no auxiliary physical atom appears in these two diagrams. Labels sit beside their correct vertices. A faint ghost D is allowed only in the right dihedral example. Keep both diagrams large and simple with four prominent spheres each, so the two diagonal endpoints are unmistakable. The coordinates are drawing instructions only; never print them. The attached FIRST SigmaDock image is the edit target. Full scientific instructions follow.\n\n'''
(a/'prompt.txt').write_text(header+s,encoding='utf-8')
for cid,anchor in [('autotool','react'),('sigmadock','diffdock')]:
    p=(PAIRS/cid/'prompt.txt').read_text(encoding='utf-8');t=(ROOT/'knowledge-base/cases'/anchor/'prompt.txt').read_text(encoding='utf-8')
    assert len(p)<=32000,(cid,len(p))
    assert len(''.join(p.split()))>=len(''.join(t.split()))
    print(cid,len(p),len(''.join(p.split())))
